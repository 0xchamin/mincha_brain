# Brain - conjectures (generated, unproven)

> Persona: **synthesizer** - re-adopt when working this file. Contract:
> [`AGENTS.md`](../AGENTS.md) § "Conjecturing on request".

> **These are not claims and must never be cited as evidence.** A conjecture is a proposition this
> brain's existing claims jointly suggest and **no source states**. It reaches
> [`claims.md`](claims.md) only after a `/research` pass returns `supports` with an **independent**
> external citation - at which point it stops being a conjecture and starts being a claim.
>
> **Every entry names its own falsifier.** That is the rule that separates this file from a
> confabulation engine: if a proposition cannot say what would prove it wrong, it is an observation
> and belongs in a topic note's synthesis or its Open questions, not here.
>
> IDs (`h1`, `h2`, ...) are stable and never renumbered. Status: `open` -> `supported` (promote and
> retire) / `refuted` (**keep** - a killed conjecture is a result) / `no-evidence`.

## Open conjectures

| ID | Conjecture | Combines | What no source states | What would falsify it | Status |
|---|---|---|---|---|---|
| h1 | **Evaluator independence requires a different model family, not merely a different invocation.** A separate evaluator drawn from the same family as the generator retains a substantial share of self-evaluation bias, because the leniency is a shared prior about what good output looks like rather than a property of grading one's own tokens. | claim 34 (do not let the producer grade its own work) + claim 36 (out-of-the-box graders are lenient QA, **biased toward AI-generated output**) | Claim 34 argues for a separate *process*. Claim 36 observes leniency toward AI output generally. **Neither asks whether separation across *families* differs from separation within one** - yet 36's bias would survive 34's fix | Run one eval suite over outputs of known quality with (a) a same-family evaluator and (b) a different-family evaluator. If the leniency gap is negligible, refuted. If (b) is materially harsher on AI-generated output, supported | `open` |
| h2 | **Prompt caching indirectly degrades output quality**, by removing the cost signal that previously constrained resident context. Teams that adopt caching will grow their context, because the thing that made them prune it was the invoice. | S10's caching finding (a price cut, **never** an attention cut) + claim 22 (limiting context beats filling it; degradation is non-uniform and starts early) | S10 says cached tokens still cost attention. Claim 22 says more context degrades quality. **Neither states the behavioural consequence**: that removing the price removes the discipline, so caching makes the quality problem *worse* by making the cost problem better | Measure resident context length in real systems before and after cache adoption. If context does not grow, refuted. Stronger test: hold context fixed and vary only caching - if quality is unchanged, the mechanism is confirmed but the behavioural claim still needs the first measurement | `open` |
| h3 | **The measured harm of episodic memory is attributable to the write being in-band, not to memory itself.** A maintained, out-of-band curated store would not show the same degradation - so claim 24 retires a *write discipline*, not a capability. | claim 24 (naive episodic scaffolding **hurt 6 of 10 models**, measured) + claim 59 (decouple curation from the work loop; one loop optimising two things trades them off silently) | Claim 24 measured append-and-retrieve. Claim 59 argues in-band curation is structurally compromised. **Nobody has connected them**: `memory.md` records the two as measuring different constructs and leaves it there. This names the mechanism that would explain 24's result | Run claim 24's benchmark with an out-of-band curated store in place of the episodic scaffold. If it still hurts 6 of 10, refuted and memory itself is the problem. If harm disappears, supported. **This is `memory.md`'s headline open question stated as something testable** | `open` |
| h4 | **The dominant failure of any model-read metadata field is vocabulary mismatch between author and consumer, and is therefore reducible editorially without changing any algorithm.** The effect size of an editorial pass should exceed that of algorithmic tuning across all three known domains. | claim 93 (metadata becomes a control surface; incumbent human vocabulary is the dominant failure) + claim 88 (retrieval quality is editorial before algorithmic) + claim 43 (a skill's description is its trigger and causes 50%+ of failures) | Claim 93 unifies three domains descriptively. Claim 88 states the editorial-first finding **for tool retrieval only**. **No source claims the editorial lever dominates the algorithmic one as a general property of model-read metadata** | Any domain where rewriting descriptions in consumer vocabulary yields materially *less* improvement than algorithmic tuning refutes it. S10 half-measured this and blended two eval regimes (`n15`); a clean single-regime comparison would settle it | `open` |
| h5 | **A self-improving agent's discovered scaffolding transfers its attack surface along with its capability, and does so super-linearly.** The tooling a loop discovers - better editing, patch ranking, retry logic - raises capability on any model it is ported to, and claim 165 says capability *is* attackability, so the transferred artifact should carry a larger security regression than capability gain on the strongest host model. | claim 174 (DGM's discovered scaffolding transfers to held-out models, and the **largest** gain was on the most capable one: Claude 3.7 Sonnet 19.0% to 59.5%) + claim 165 (more capable models are measurably **easier** to attack) | Claim 174 measures capability transfer and never mentions security. Claim 165 measures attackability against model capability and never mentions transferred scaffolding. **Neither asks whether a *portable capability artifact* is also a portable vulnerability artifact** - which matters because scaffolding, unlike a fine-tune, is meant to be shared | **Both artifacts are open source.** Run base and DGM-discovered agents through AgentDojo on the same host model. If targeted attack success is flat or lower for the improved agent, refuted. If it rises, supported; if it rises *faster* than the capability gain, supported in the strong form | `open` |
| h6 | **Provenance tracking is worth more as a recovery control than as a preventive one, and the field has proposed it for the wrong job.** Prevention requires deciding at write time whether content is malicious, which claim 159 shows is undecidable for weak-signal payloads. Recovery requires only knowing **where an entry came from**, which is decidable by construction. | claim 163 (two independent groups converged on provenance tracking, both proposing it **preventively**) + claim 159 (detection fails structurally on payloads that look like legitimate content) + claim 176 (S22's archive gives a **traceable lineage of modifications for review**) | Claim 163 records the convergence and frames both as prevention. Claim 159 shows why prevention by inspection cannot work. Claim 176 describes lineage as a review aid. **No source states that the undecidability of the first makes the third the actually load-bearing use** - nor that provenance's value is asymmetric between the two jobs | Build a store with write-path provenance and no admission control. Poison it with S19's weak-signal payloads, then attempt source-scoped quarantine after the fact. **Refuted if recovery fails** - most plausibly because derived entries (compaction summaries, synthesised skills) lost their lineage, which would be the interesting negative result. Refuted differently if a provenance system *does* prevent weak-signal poisoning at acceptable false-positive rates | `open` |
| h7 | **The 17% is a property of the task distribution, not of the defence - so every isolation defence's ceiling is set by your tool granularity, and is a design variable rather than a constant.** The tool filter fails exactly where the user's task needs the same capabilities the attack needs, so the failure rate should track the **read/write capability overlap** of the tool surface and fall as tools are split finer. | claim 167 (a tool filter drops attack success to 7.5% and fails on **17%** of cases where the task's own tools suffice for the attack) + claim 166 (attack success is a property of the **application**, not the model, predicted by how much tool output the attacker controls) + claim 155 (CaMeL protects actions, not assertions) | Claim 167 reports 17% as a bound on the defence. Claim 166 establishes that attack properties live in the application. **Nobody connects them to say the 17% is a measurement of AgentDojo's tool suite** rather than of the tool filter - which converts a reported ceiling into something a builder controls | Measure read/write capability overlap across task suites with deliberately different tool granularity, and check whether isolation-defence failure rate tracks it. **Refuted if failure rate is roughly constant** across suites whose overlap differs substantially. **TESTED 2026-08-05 against AgentDojo's own published run data** ([script](../reports/experiments/260805_h7_agentdojo_test.py)): under the tool filter, un-isolatable pairs are attacked **46.7%** of the time against **0.2%** for isolatable pairs - a **220x risk ratio**, chi-square 227.2, n=547. **The control is what makes it convincing**: with *no* defence the same split predicts nothing (50.7% vs 47.9%, chi-square 0.2), so un-isolatability is not a proxy for general attackability - it becomes predictive only once the filter is applied, which is exactly the claimed mechanism. **35 of the 36 attacks surviving the tool filter are un-isolatable pairs.** | **`supported`** - promoted as **claim 178**. Retained here rather than retired, because the supporting analysis is **this brain's own** over a third party's artifacts, not an independent publication |
| h8 | **The amplification hazard is a measurement-coverage problem rather than an alignment problem, and therefore has an eval-side fix.** If a self-improving loop compounds only what its benchmark cannot see, then extending the benchmark to cover a property should stop that property degrading - and the interesting outcome is if it does not, because that would mean the loop finds ways to satisfy the extended measure while still degrading, which is Goodhart rather than blindness. | claim 177 (three independent sources: a loop optimises what it can measure and compounds what it cannot) + claim 164 (in an adversarial evaluation the judge must be deterministic, because a model judge shares a vulnerability with what it grades) + claim 124 (verification, not generation, is the rate limit) | Claim 177 states the mechanism and stops. **No source asserts where the fix belongs.** S22's four safeguards are all containment, explicitly not correctness, and the paper says a correctness check is impossible - so the eval-side hypothesis is available and untested | Add a security suite to a self-improving loop's evaluation and run it long enough for the compounding claim to apply. **Supported if the covered property stops degrading. Refuted if it degrades anyway**, which would locate the problem in optimisation pressure rather than in coverage - a far more important result, and the reason this is worth running even though the mechanism sounds obvious | `open` |
| h9 | **Drift in a compounding knowledge base accumulates with the number of cross-references, not the number of sources** - so the maintenance cost of this design is super-linear in coverage, and reconciliation frequency must scale with connectedness rather than with ingest count. | claim 177 (a loop compounds what its verifier cannot see) applied to **this kit** + the two dream passes as data: dream 0001 found 6 defects over 11 sources, dream 0002 found 8 over 11 more, and **both passes' findings clustered in the most heavily cross-referenced notes** (`agents.md`, `agent-security.md`) rather than being spread evenly | `validate.py` checks form and structurally cannot see cross-note factual drift, which is the brain's own instance of claim 177. **No source, and no note, states that the drift rate should scale with edges rather than nodes** - and the two passes' distribution is a hint, not a measurement | Record findings per pass against both source count and cross-reference count over several more passes. **Refuted if per-source drift stays flat as the brain grows** - which would make it linear and mean a fixed reconciliation cadence suffices. This repo generates the data for free, which makes it the cheapest test in the file and the only one testable without external evidence | `open` |

## Discarded at generation

Recorded because a pass that keeps only its winners is cherry-picking, and because a discarded
candidate stops being re-proposed next pass.

| Candidate | Why discarded |
|---|---|
| "Contract documents are the load-bearing artifact of LLM knowledge systems" (claims 68 + 93) | **Restates claim 68.** Combining it with 93 adds emphasis, not a proposition - no new assertion, so nothing to falsify |
| "Multi-agent systems need versioned stores" (claim 61 + S12's tenant isolation) | **No falsifier.** As stated it is a design preference; every plausible test collapses into "did a bad thing happen", which is unfalsifiable in the absence of an incident |
| "Ablation generalises to any expiring scaffold" (claims 31 + 44) | **Already a claim.** Claim 44 says the delta is the verdict and claim 31 names the retirement question; the union is what `skills.md` already asserts |
| "Agents will converge on filesystem-shaped interfaces" (claim 60 + S7's memory-as-files bet) | **Not researchable on any horizon.** A prediction about where the field goes, with no evidence that could arrive in time to be useful. Interesting; not a conjecture |
| "MCP is the agent's primary attack surface" (claims 143 + 166 + `mcp.md`'s scope) | **Restates claim 143 with a noun swapped in.** Indirect injection arrives through whatever channel carries untrusted content; naming MCP as *the* surface asserts nothing 143 and 166 do not already, and every test collapses into claim 166's application-dependence |
| "Self-improving agents need security benchmarks" (claim 177 + claim 168) | **An implication, not a conjecture.** It follows directly from 177 with no additional content, and it has no falsifier distinct from h8's, which states the interesting version - *whether* extending the benchmark actually works |
| "Provenance should span session boundaries" (claim 163) | **Already an open question, not a conjecture.** `agent-security.md` records the gap between S18's execution-scoped and S19's write-path provenance verbatim. Restating a recorded gap as a proposition adds no assertion; **h6 is what that material actually supports**, because it says something about which *job* provenance is for |
| "A more capable defence is also easier to attack" (claim 165 inverted) | **No mechanism and no falsifier.** Claim 165's mechanism is that a weak model fails at the attacker's multi-step goal too; nothing in it transfers to defensive components, and the symmetric-sounding statement has no evidence that could arrive either way |

## Passes

| # | Date | Scope | Generated | Discarded | Note |
|---|---|---|---|---|---|
| 1 | 2026-08-03 | whole brain, **partial** | 4 | 4 | First pass, and the stage's first run. **Scope caveat: generated from `claims.md` plus the topic notes read in that session, not a fresh read of all nine.** A full pass should re-run over every topic note's Open questions, which is where the richest unclaimed material sits. Incidentally surfaced a mis-citation (claim 44 cited for the skill-description trigger in four places; the claim is 43), fixed separately |
| 2 | 2026-08-05 | whole brain, **full** | 5 | 4 | **The first full pass**, and the first with the Open questions of all eleven notes actually read - which is what pass 1 flagged as its own gap. Run immediately after [dream 0002](dreams/0002-260805.md), on a brain that had doubled since pass 1: **claims 1-100 became 1-177 and 11 sources became 22**, almost all of it security. **Four of the five conjectures combine a 2026-08-04/05 source with something older**, which is the pattern the contract predicts - new material generates against the existing stock rather than against itself. **h5 and h9 are the two worth running first**: h5 because both artifacts are open source and the experiment is a weekend, h9 because this repo already generates the data and it is the only entry testable with no external evidence at all. **No conjecture here required a new source; all five were available from material sitting in the brain unremarked.** |
| - | 2026-08-05 | **h7 tested, not a generation pass** | - | - | **The first conjecture in this file to be tested rather than proposed**, and the first result this brain produced itself. h7 is `supported` and promoted as claim 178. Method: AST-parse AgentDojo's ground-truth tool sets, mark a (user, injection) pair un-isolatable when the injection's tools are a subset of the user task's, and read the shipped run data for the no-defence and tool-filter pipelines. **Reproducible in one file** - [`reports/experiments/260805_h7_agentdojo_test.py`](../reports/experiments/260805_h7_agentdojo_test.py) - with no model calls and nothing re-run. **A per-suite aggregate test was run first and was inconclusive** (Spearman +0.80 at n=4, confounded by baseline attack rate, and the discriminating residual-ratio test pointed the wrong way at -0.40). **Aggregation was hiding the effect**; the per-pair unit gave n=547 and settled it. Recorded because the misleading intermediate result is the part a later reader would otherwise repeat. |
