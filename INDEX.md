# Brain - INDEX (ask here)

> **This is the entry point.** To ask a question of the brain - at the repo root - **start here**:
> this catalog tells you which source or topic covers what, so you (or the agent) read the right
> notes instead of every file. It owns the **brain-wide** scope (all sources + all topics); each
> source's own map lives in its `SOURCE.md` "Reading order", and each topic's detail in
> `brain/topics/*.md`. Keep it a signal store, not a dump.
>
> **Auto-maintained.** This file is a **hard output** of ingest, compound, and close-loop - the
> agent rewrites the affected rows every time media is added, a claim is promoted, or the loop is
> closed. It is **not** hand-curated between those checkpoints.
>
> **Integrity rule (agent must uphold):** every `sources/<folder>/` has **exactly one** row in the
> Sources table below, and every `brain/topics/*.md` has **exactly one** row in the Topics table.
> If a source or topic exists on disk but not here, it is unfindable - add the row. If a row points
> to a folder that no longer exists, remove it.
>
> **Annotate every entry** with a one-line summary + "when to read" - a bare list forces a reader to
> open each link to judge relevance; annotating once does that upfront.
> (Principle from eugeneyan.com/writing/working-with-ai - the annotated INDEX.)
> (No owner column - this is a single-person brain; everything here is yours.)

## Sources

| Source | Type | Summary | Topics | When to read | Folder |
|---|---|---|---|---|---|
| 12-Factor Agents: Patterns of reliable LLM applications (Dex Horthy / HumanLayer, AI Engineer WF 2025) | video | **Start here for agent fundamentals.** An agent is a prompt + switch statement + context builder + loop - own all four. Tools are just structured JSON; what ships in production is micro agents (3-10 steps) inside deterministic code, with humans contacted via tool calls. | agents, context-engineering | First principles before building any agent; deciding what to own vs delegate to a framework; why your agent stalls at 70-80%; pause/resume design. | [`sources/260725_12-factor-agents/`](sources/260725_12-factor-agents/LEARNING.md) |
| Building Closed-Loop Evals for a Multimodal Agent at Scale (Uber, AI Engineer WF 2026) | video | Blueprint for evaluating an agent pipeline in production: log-first, per-stage metrics (routers as classifiers, generation pass@k, pairwise comparison), Swiss-cheese QA gates, and a closed self-tuning loop on drift. | evals, agents | Designing evals for an agent/LLM pipeline; router precision-recall; pass@k; auto-tuning on drift. | [`sources/260725_closed-loop-evals-multimodal-agent/`](sources/260725_closed-loop-evals-multimodal-agent/LEARNING.md) |
| Harness Design for Long-Running Application Development (Prithvi Rajasekaran / Anthropic Labs, 2026) | blog | **A harness built, costed, then half-deleted on a newer model.** Planner/generator/evaluator; the split exists to beat **self-evaluation bias**, not to add capability. Subjective quality becomes gradable by fixing the *question*. Names "context anxiety" and why compaction does not fix it. 18x time / 22x cost vs a solo agent - which produced a broken app. | agents, context-engineering, evals | Designing or pruning agent scaffolding; deciding whether a component is still load-bearing; building an evaluator; long-run context strategy. **⚠️ T2 vendor source, n=1 self-reported runs, visual leg skipped - mechanisms trustworthy, numbers not replicated.** | [`sources/260725_harness-design-long-running-apps/`](sources/260725_harness-design-long-running-apps/LEARNING.md) |
| Don't Ship Skills Without Evals (Philipp Schmid / Google DeepMind, AI Engineer WF 2026) | video | **The brain's first source on skills, and it arrives with third-party numbers.** A skill is a three-layer cost ladder, not a document; its description is the trigger and causes 50%+ of all failures; length is an inverted-U peaking at 200-500 lines, and AI-written skills are a *negative* intervention (-8 to -11 pts). Retirement by **ablation** - and keep the eval after you delete the skill. | skills, evals, agents | Before writing or reviewing any skill; designing evals for an instruction artifact rather than a pipeline; deciding when scaffolding has expired. **⚠️ Split the evidence: SkillsBench is a public third-party benchmark (strong); the DeepMind-internal figures are self-reported n=1.** | [`sources/260726_dont-ship-skills-without-evals/`](sources/260726_dont-ship-skills-without-evals/LEARNING.md) |
| Dreaming: Better memory for a more helpful ChatGPT (OpenAI, 2026) | blog | **The brain's first source on memory.** Memory written during a conversation keeps that conversation's tense, so it decays into *confident wrongness*, not irrelevance. The fix is architectural: move the write off the turn into a background synthesis pass, store a maintained narrative rather than an append-only fact list, and **revise** stale entries instead of expiring them. Correction is offered on the synthesized summary, not the raw records. Its own evals put staleness at a **9.4%** baseline - wrong nine times in ten - which is what makes the diagnosis more than rhetoric. | memory, context-engineering, evals | Designing any cross-session memory; deciding between a fact store and a maintained artifact; building an eval for memory (the three-objective frame is the most portable part). **⚠️ T2 vendor post on its own consumer product. Numbers are exact (recovered from the page's Vega-Lite chart specs) but "task success" is undefined and no sample size or method is published - directional self-report, not a benchmark. Do not borrow them to settle claim 24, which measures a different memory design on a different system class.** | [`sources/260731_chatgpt-memory-dreaming/`](sources/260731_chatgpt-memory-dreaming/LEARNING.md) |
| Memory and dreaming for self learning agents (Anthropic, "Code w/ Claude", 2026) | video | **The agent-platform half of the memory pair, and the independent counterpart to the OpenAI post above.** Same architecture, same name, different vendor: agents write memory during work, and a decoupled batch pass rewrites it between sessions. **The reason to split the loops is objective conflict, not throughput** - one loop asked to both finish the task and curate memory trades them off untunably. Memory is deliberately a **file system** the model drives with `bash`/`grep`, on the same bet that produced skills. Adds everything a consumer assistant never needs: scoped stores, optimistic concurrency via a `content_sha256` precondition, per-session attribution. A live demo shows agents leaving **instructions** for their successors, not just facts. | memory, agents, agent-security | Designing memory for a multi-agent system; deciding what to run out of band and why; anything touching shared-store arbitration or memory as an attack surface. **⚠️ T2 vendor talk on its own product, and the weaker evidence of the pair - every number is a customer testimonial with no baseline or method. It was the source that could have settled whether *maintained* memory helps an *agent* (claim 24) and does not. Read it for the architecture, never for the numbers.** | [`sources/260731_claude-memory-dreaming/`](sources/260731_claude-memory-dreaming/LEARNING.md) |
| LLM Wiki (Andrej Karpathy, personal gist, 2026-04-04) | blog | **The brain's first source on RAG, and it argues against it.** Retrieval is *stateless across queries* - a synthesis question re-pieces the same fragments every time, at the moment the user waits, and keeps none of it. The alternative: **compile the knowledge once** into a maintained markdown wiki an LLM owns, paying synthesis at ingest time. Three layers defined by **who may write** (immutable raw / LLM-owned wiki / co-evolved schema document), three operations (**ingest, query, lint**), and the non-obvious rules - ingest *integrates* rather than indexes, queries file their answers back as pages, and **lint is a periodic out-of-band pass over the whole store**. The pattern is Bush's **Memex** (1945), which was blocked on one thing: who does the maintenance. | rag, memory, context-engineering | Designing any LLM-maintained knowledge base; deciding between retrieval and a compiled layer; before writing an `AGENTS.md`-style contract document. **Also read it as a mirror of this kit** - and read [ADR-0010](brain/decisions/0010-lint-is-the-dream-pass.md) with it, because ADR-0009 cited this gist before ingesting it and got it backwards. **⚠️ T4, ~1,960 words, no figures, no code, no data - every claim is `single-leg` by construction and nothing here is measured.** Two efficacy claims are assertion (`n10`, `n13`) and one contradicts the document's own operations section (`d1`). Its unusual virtue: nothing is being sold. | [`sources/260731_llm-wiki/`](sources/260731_llm-wiki/LEARNING.md) |
| Inside the Microsoft Agent Framework: How we designed a layered SDK (Shawn Henry, 2026) | blog | **A vocabulary, not a finding - and the brain's first flat cross-source contradiction.** Three separable ideas: the **agent loop** (execution), **workflows** (orchestration), the **harness** (runtime capabilities) - and the summary figure shows they are *not a stack*, so the loop is the only mandatory layer. Supplies the only **enumerated harness inventory** here (tools / context / planning / middleware), files `skills` beside prompts and memory, names `todo` a planning primitive, and ships the generator-evaluator split as an **Author/Critic** SDK pattern. Its figures outrun its prose - an agent-provider slot takes a whole third-party agent (Claude Code, GitHub Copilot CLI). | agents, context-engineering, skills | Naming the parts of an agent system; deciding what a harness should contain (then read claim 31 for what to delete); orchestration pattern vocabulary. **⚠️ T2 vendor design post about its own SDK: nothing measured, nothing compared, and it contradicts claim 12 head-on (`d1`) without acknowledging the position exists. Four harness items are figure-only.** | [`sources/260801_agent-framework-layered-sdk/`](sources/260801_agent-framework-layered-sdk/LEARNING.md) |
| Tool search: Finding the right tool at the right time (Lisa Brown Jaloza / Microsoft, 2026) | blog | **The brain's first real MCP source, and it is an article about something else.** A tool catalog stops being schema management and becomes a **search problem** past ~10-15 tools. Replace `tools/list` with two meta-tools and the catalog stays indexed but never listed: **541k tokens to 15k at 1,180 tools (36x)**, with the tool-search curve roughly **flat** as the catalog grows 24x. The reframing is the real payload - **tool names and descriptions become ranking features**, so the first tuning pass is **editorial, not algorithmic**. Retrieve the long tail (where the rare, high-stakes tools live), **pin the head**. | mcp, rag, context-engineering, agents | Any agent past a dozen tools; deciding what the model should *see* vs what it can *do*; the cost model of `tools/list`; retrieval quality when the retrieved items are capabilities. **⚠️ T2 vendor post on a preview product, but better evidenced than that class usually is - a public benchmark (ToolRet), an honest baseline, a reported loss. Three caveats: the Recall@10 comparison borrows its baselines from another paper under a possibly different protocol (`d2`); the metadata-tuning percentages are method-free self-report; and the source never confronts its own headline - Recall@10 of 39-46% against a default shortlist of 5.** | [`sources/260801_tool-search-toolboxes/`](sources/260801_tool-search-toolboxes/LEARNING.md) |
| How we built LangChain's agent-first data stack (Emily Hawkins / LangChain, 2026) | blog | **The brain's first source on an agent reading a company's own proprietary knowledge - and the figure gives away more than the prose.** The architecture diagram captioned as the "big architectural shift" is a stock ELT pipeline with **no agent in it**: what changed is the reporting tool and the English written around the warehouse (`d1`). The context layer decomposes into **five stores split by the question each answers** - what the data is, what a metric means, how the business works, which source to trust, how a number is computed. The transferable move: **a column definition stops being a description and becomes an instruction**, carrying a default filtering rule the agent applies - the third domain in this brain where human-facing metadata is quietly promoted to a control surface. Trust signals need a **writer restriction** ("if everything is endorsed, the signal stops being useful"), and the loop's output is a **write back to the context store, never an answer**. | context-engineering, rag, evals, skills | Designing the context layer for an agent over any proprietary domain; writing definitions an agent acts on; adding a trust signal to a knowledge store; deciding whether to curate the head or retrieve the tail. **⚠️ T4 practitioner experience on a T2 vendor blog (LangChain writing about LangChain, on a stack bought from Hex - functionally also a testimonial). n = 1 company, ~290 people. Every number measures *adoption* and the thesis is about *trust*, which is measured nowhere - the authors concede it (`d4`, `n10`), and the 40x compares mismatched units (`d3`). Its weight comes from the research pass, not the article: claims 93 and 95 are the first here to reach `corroborated` on independent external evidence.** | [`sources/260802_agent-data-stack/`](sources/260802_agent-data-stack/LEARNING.md) |
| Multi-tenant agentic AI system (Google Cloud Architecture Center, reviewed 2026-06-18) | blog | **The brain's first source on deploying agents across an organisation rather than building one - and the lesson is in its second half, not its diagram.** Put the tenant boundary at the platform's own coarsest unit (a project per business unit) and bound the agent's identity with a **Principal Access Boundary**, because you cannot police a request the model composed at run time from attacker-influenceable text. That one boundary buys confidentiality, blast-radius and noisy-neighbour isolation together. **Then every cheaper alternative it offers is the same trade: sharing a component converts a structural guarantee into an enforcement obligation nobody has specified for agents** - including a cost recommendation that deletes the tenant-local PII filter its own security section calls essential (`d2`). | agent-security, mcp, agents, context-engineering | When designing agent infrastructure for more than one team, or arguing about per-tenant vs shared anything. Read §8-9 first and the architecture second. Also read it for what a 2026 enterprise reference architecture leaves out: **no evals, no cross-tenant path, no memory**. **⚠️ T2 vendor, and completely unmeasured** - no latency, cost, incident or named deployment, and both corroboration legs are the same team's. A shape, never a result. | [`sources/260802_gcp-multi-tenant-agentic-ai/`](sources/260802_gcp-multi-tenant-agentic-ai/LEARNING.md) |
| OAuth 2.0 and OpenID Connect (in plain English) (Nate Barbettini / Okta, 2018) | video | **The clearest explanation of the delegated-authorization substrate.** OAuth exists to kill password sharing; its shape follows entirely from "the browser can talk to a human but cannot hold a secret". Four flows = four answers to which channels you have. OIDC is a ~5-10% layer adding an ID token, triggered by one scope. | agent-security | Before reasoning about agent permissions, MCP auth, or any consent design; when you need scopes/tokens/PKCE to actually click. **⚠️ 8 years old - mechanics current, but it recommends the implicit flow for SPAs, which the field has since reversed (`n17`). Read "What has aged" first.** | [`sources/260725_oauth2-oidc-plain-english/`](sources/260725_oauth2-oidc-plain-english/LEARNING.md) |
| autoresearch (Andrej Karpathy, code, snapshot `228791f`, 2026-03) | code | **The brain's first source on an agent that runs experiments unattended, and the first where the whole design is a permissions diagram.** An agent gets one editable training script, a fixed 5-minute wall-clock budget, a protected metric and an instruction never to stop; overnight it runs ~100 experiments and keeps what improves the number. The transferable object is the **containment design, not the ML**: the four things you freeze before letting an agent change everything else (the editable surface, the resource budget, the metric's units, the holdout), git as the experiment database with `git reset` as discard, and the ledger deliberately kept **outside** git because the loop rewinds the tree. Two weaknesses both sit on the *decision* path rather than the computation path: the protected score is printed by the file the agent rewrites, and **the accept rule has no notion of variance - which is why the last of 15 kept "improvements" in the author's own published run is a change of random seed.** | autonomous-research-loops, evals, agents, context-engineering | Before building any unattended optimize-and-accept loop, in any domain - the four freezes are the reusable part. Read §5 and §9 of the note if nothing else. Also the cheapest available lesson in why an automated accept rule needs a noise floor. **⚠️ T4 personal repo; the design is fully inspectable and every design claim passes the docs-vs-code gate, but the *results* are one unreproducible PNG (the ledger is untracked by design and absent from the repo), no external evidence was gathered, and this brain ran none of the code (no GPU).** | [`sources/260803_autoresearch/`](sources/260803_autoresearch/LEARNING.md) |
| Stanford CS329A: Self-Improving AI Agents - Part 1, Course Overview (Azalia Mirhoseini + Akanksha Chowdhery, Stanford Online, 2026) | video | **The brain's first academic source, and its first on a model improving itself.** Self-improvement turns out to be **loop closure rather than a technique**: sample the model many times, keep what survives a check, fine-tune on the survivors, repeat - one arrow added to the ordinary training pipeline. The reusable vocabulary is the **coverage/precision split** (can a correct solution be generated at all, versus can it be identified), and the reusable *finding* is that **verification, not generation, sets the ceiling** - which is why gains sort almost exactly by how mechanisable a correctness check is, from **below 50%** on personal writing to **~72%** on mathematical calculation. Also the second independent statement in this brain that **models prefer their own reasoning traces**, and the first case of the verifier being written by the system it judges. | self-improvement, evals, agents, autonomous-research-loops | Before building anything that improves on its own output, in any domain - the coverage/precision split and the verifier-sets-the-ceiling claim are the transferable parts. Read §5 and §6 of the note if nothing else. Also the cheapest available correction to "test-time scaling makes small models beat big ones". **⚠️ Lecture 1 of a course, so a map rather than a result** - it defers nearly every mechanism to a later session. **Independence fails twice over on its two strongest slides**: the repeated-sampling chart is the presenter's own preprint (T3) and the o1 charts are vendor promotional material (T2) with unlabelled axes. **Its headline slide materially overstates its own chart** (`d1`) - it plots *coverage*, not accuracy, and half its panels assume an oracle. The lecturers' own admission that "that whole loop is not completely well understood" is the most trustworthy thing in it. | [`sources/260804_cs329a-self-improving-agents/`](sources/260804_cs329a-self-improving-agents/LEARNING.md) |
| Stanford CS329A: Self-Improving AI Agents - Part 2, Test-Time Compute Scaling (Azalia Mirhoseini, Stanford Online, 2026) | video | **The mechanism lecture 1 kept deferring, and the best thing in it is the demolition rather than the technique.** Repeated sampling is *lawful* - coverage against sample count fits `c = exp(a·k^b)` from 70M to 70B parameters, so an inference budget becomes an estimate rather than a guess - and the reason is a genuinely surprising theorem: per-problem success is *exponential* in k, so a power law in the average can only come from **a long tail of very hard problems**, making a scaling exponent a fact about your *benchmark* first. Then the lecture spends its best twenty minutes destroying the naive reading. Sampling hands you a **set** containing a right answer, and every deployable way of picking one out **plateaus after 10-50 samples** while coverage climbs three more orders of magnitude - **0.40 against 0.95 on MATH** - because on the hardest problems the correct answer turns up **1-3 times in 10,000 samples** and is invisible to any selector that works by agreement. The escape is to stop selecting: **fusing all k candidates into one answer beats picking the best candidate with a perfect oracle.** | self-improvement, evals, agents | When you are deciding whether to spend money on sampling rather than on a bigger model, and **before quoting anyone's repeated-sampling result at all**. The reading rule it teaches by accident is the durable part: check whether a chart plots **coverage** or **pass@1** before believing a comparison drawn on it. Read §6 and §7 of the note if nothing else. **⚠️ Not independent of S14** - same course, same lecturers, so it supplies mechanism and cannot raise anyone's confidence; no topic status moved. **The presenter is senior author of 3 of the 4 papers taught**, so every efficacy number is self-report, and the two most reusable findings (fusion-beats-oracle, the cheap exponent method) are the two gated weakest. **The gate caught the source overstating its own headline three times and then refuting itself sixteen minutes later** (`d1` against `n10`) - which, with `n31`, is what turned that pattern into claim 132 rather than a complaint. | [`sources/260804_cs329a-test-time-compute/`](sources/260804_cs329a-test-time-compute/LEARNING.md) |
| AgentPoison: Red-teaming LLM Agents via Poisoning Memory or Knowledge Bases (Chen, Xiang, Xiao, Song, Li - U Chicago / UIUC / U Wisconsin / UC Berkeley, arXiv 2024-07-17) | paper | **The brain's first paper source, its first measured attack, and its first source with nothing to sell.** An agent's memory or knowledge base is an input nobody treats as one: retrieved records enter context as demonstrations, and the selection is made by **embedding geometry rather than by any judgement about trust**. AgentPoison optimises a short trigger so that any query containing it lands in a region of the retriever's embedding space that is **unique** (far from benign queries) and **compact** (all triggered queries together), then puts the poison at those coordinates - so retrieval succeeds by construction instead of by out-competing the corpus. **No model access and no training: the optimisation runs against the embedder.** The numbers that matter are not the headline: **one poisoned record gives ~62% retrieval success and a one-token trigger ~79%**, benign accuracy above 90% throughout, at a poisoning ratio under 0.1%. Triggers **transfer to embedders they never saw**, including a black-box commercial API, so a private embedder is not a mitigation. And the trigger reads as ordinary language - *"Be safe and make a discipline."* - which defeats perplexity filtering by removing the property the filter measures. | agent-security, memory, rag | Before designing any agent that retrieves from a store it does not fully control, and **before relying on volume anomaly detection, embedder privacy, perplexity filtering or isolate-then-aggregate** - claims 138-141 close off all four. Read §5 and §8 of the note if nothing else. Also the adversarial counterpart to the three memory sources here, none of which attacked the design they converged on. **⚠️ T3 preprint** - the PDF reads "Preprint. Under review." and the arXiv listing carried no journal reference at ingest. Independence is unusually good (five authors, four universities, no vendor) and that does not remove the ordinary incentive to present one's own method well: **the benign-cost headline is an average hiding a 4-point worst case (`d1`), the end-to-end success rate exceeds the action success rate threefold on two agents with no explanation (`d2`), and the baselines are the authors' own adaptations.** The defeat of isolate-then-aggregate is **argued, never run** (`n10`, `single-leg`). Everything is internal to one paper - no external evidence, companion repo not cloned. | [`sources/260804_agentpoison/`](sources/260804_agentpoison/LEARNING.md) |
| Not what you've signed up for: Compromising Real-World LLM-Integrated Applications with Indirect Prompt Injection (Greshake, Abdelnabi, Mishra, Endres, Holz, Fritz - Saarland / CISPA / sequire technology, arXiv 2023-02-23) | paper | **The paper that named indirect prompt injection, and the source that moved `agent-security` to `established`.** Its one load-bearing sentence: **processing untrusted retrieved data is analogous to executing arbitrary code**, because retrieval puts data and instructions in one undifferentiated channel and there is no parameterised prompt. The attacker therefore never touches your system - **no account, no request, no rate limit** - they place text where your agent will read it, and your own application fetches it. From there the classical playbook transfers intact and is demonstrated on **real products** (Bing Chat on GPT-4, GitHub Copilot): prompts that **forward themselves to your contacts**, that **write themselves into the agent's memory and re-poison a later session**, that **fetch fresh instructions from the attacker's server each request**, and that stage a tiny public payload to pull a larger private one. Two findings make the economics worse: **the attacker states only the goal and the model invents the method** (Bing generated its own social engineering unprompted), and the model's **follow-up API calls retrieve material that reinforces the injection**. And the defensive lesson: **Bing Chat filtered its chat channel and not its retrieval channel** - the filter was real and on the wrong side. | agent-security, memory, agents | Before building anything that reads content it did not author, which is every agent with retrieval or tools. Read §3 and §8 of the note if nothing else. Pair it with S16: **the two corroborate on agent memory as a persistence surface from opposite directions**, which is what [ADR-0019](brain/decisions/0019-agent-security-established.md) rests on. **⚠️ T3 preprint** (no journal reference on the arXiv listing at ingest). Independence is unusually strong - six authors, three institutions, no vendor, responsible disclosure to OpenAI and Microsoft, no author overlap with S16. **But it is entirely qualitative: no success rate, no sample size, no statistics for any of six threat classes** (`d1`), and the most striking results run against a black-box product the authors concede they cannot reproduce (`d2`). **Its mitigations survey is three years old and is its weakest material** - read it as "no solution existed in early 2023 and here is why each obvious one is hard", never as a current statement. | [`sources/260804_indirect-prompt-injection/`](sources/260804_indirect-prompt-injection/LEARNING.md) |

| Defeating Prompt Injections by Design - CaMeL (Debenedetti, Shumailov, Fan, Hayes, Carlini, Fabian, Kern, Shi, Terzis, Tramer - Google / Google DeepMind / ETH Zurich, arXiv 2025-03-24) | paper | **The brain's first gated *defence*, and the first source here whose security argument does not depend on the model behaving.** The thesis is a reversal: stop making the model safe, and build a system in which **an unsafe model cannot cause an unsafe action** - by importing three decades-old ideas (Control Flow Integrity, information flow control, capabilities), each of which puts the security decision somewhere the untrusted component cannot reach. It first shows why the obvious structural fix is not enough: Willison's **Dual LLM** protects the **plan** and not the **arguments**, so an injection leaves the action sequence exactly as the user asked while the fetched file becomes `confidential.txt` and the recipient becomes the attacker - **SQL injection against the query parameters rather than the structure**, which also names the fix. CaMeL applies parameterisation to **the program** instead of the prompt: a Privileged LLM sees only the trusted query and writes code, **never seeing tool output**; a Quarantined LLM parses untrusted data with no tools and may return **only schema-conforming output plus one boolean**, because a free-text reply would carry the injection back into planning. Every value then carries a **capability** (provenance + permitted readers), an interpreter propagates it through a data-flow graph, and a **Python security policy** is checked at every tool call - so `send_email` refuses, and **nothing in the chain required the model to notice the attack**. | agent-security, agents, context-engineering | When you are designing defences rather than cataloguing attacks, and **before adopting any prompt-injection mitigation** - its cost comparison (2.82x tokens against Spotlighting's 1.06x) is the honest framing of the choice between probabilistic-and-nearly-free and structural-and-expensive. Read S17 first; this is the answer to it. Read §2 and §9 of the note if nothing else. **⚠️ T3 preprint, and its efficacy numbers are measured on AgentDojo - whose first author is CaMeL's first author, with Tramer co-authoring both, and whose baseline defences CaMeL's authors implemented** (`d1`, `d3`). Self-report, not validation: claim 153 is `needs-check` while the design claims are not. Note the vendor position too - the thesis that scaffolding beats model-hardening favours a platform provider. **And read claim 155 before assuming it covers your threat model: it protects *actions*, not *assertions*, so fraud and manipulated content are explicit non-goals.** Unusually honest - it demonstrates a bypass of its own isolation (§6.4), predicts a return-oriented-programming analogue, and titles §9.3 "So, Are Prompt Injections Solved Now?" answering "No". | [`sources/260804_camel-prompt-injection-defense/`](sources/260804_camel-prompt-injection-defense/LEARNING.md) |

| From Untrusted Input to Trusted Memory: A Systematic Study of Memory Poisoning Attacks in LLM Agents (Dash, Ge, Jain, Shah, Shang - Huawei Canada + University of Waterloo, arXiv 2026-06-03, AIWILD workshop at ICML 2026) | paper | **The first source anywhere here that enumerates how a poisoned memory write actually happens** - and the third independent leg of claim 145, after S16 and S17. The economics first: prompt injection needs its payload present every time it fires, **memory poisoning needs one successful write**, after which the entry is retrieved in later sessions as trusted prior knowledge. Then the surface: memory is written through **four channels of which only one is an explicit command** - the other three fire on a system-prompt retention policy, a compaction threshold, or the agent deciding a finished task was a reusable skill, with **the model's own judgement as the write authority**. You cannot filter an instruction nobody issued. The organising finding is about **detectability**: attacks split into strong-signal (recoverable linguistic markers) and **weak-signal** payloads that read as ordinary domain content - *"For continued WiFi connections, re-enter credentials at network-auth.xyz-helpdesk.com is standard procedure"* contains no instruction and is stored for satisfying a retention policy. Measured against that, four production injection detectors give incomplete coverage, **none achieves both high recall and low false positives**, and **retraining made the strongest one worse** (67.67% to 61.60%), which the authors read as structural. And the design lesson: **agents that write and retrieve memory more aggressively are proportionally easier to poison** - roughly 2x, holding the model constant. | agent-security, memory, skills | Before designing any agent memory, and **before trusting an injection guardrail to cover it**. Read §4 and §8 of the note if nothing else. **§8 is the one a builder of a self-improving agent must not skip**: V-S5 says a poisoned skill is not static, because the refinement loop treats "executed without error" as validation and **optimises the adversarial procedure over time** - which is claim 114 with an adversary choosing the noise. **⚠️ T3 with workshop review. Its benchmark hands the payload to the agent as a labelled block beside the user query rather than routing it through a real tool call** (`d1`, disclosed by the authors), so the numbers measure how permissive an agent's write and retrieval policies are, not how easily an attacker reaches them. One model throughout (`d2`), four of five authors Huawei Canada (mild vendor position), and **the most consequential claim (V-S5) has no measurement behind it**. Note also the scope correction in `d3`: four **detectors** were tested and no structural defence was, so this does **not** refute S18. | [`sources/260805_memory-poisoning-systematic/`](sources/260805_memory-poisoning-systematic/LEARNING.md) |

| AgentDojo: A Dynamic Environment to Evaluate Prompt Injection Attacks and Defenses for LLM Agents (Debenedetti, Zhang, Balunović, Beurer-Kellner, Fischer, Tramèr - ETH Zurich + Invariant Labs, NeurIPS 2024 Datasets & Benchmarks Track) | paper | **The field's reference benchmark for agent security, and the first artifact here built to settle arguments rather than win one.** A user task and an attacker task run in the **same stateful tool-calling environment** and are scored separately - 97 user tasks crossed with injection tasks giving 629 security cases, 70 tools, four realistic applications. **Its most transferable idea is a design decision**: utility is checked by **deterministic functions, never an LLM judge**, because an attack strong enough to hijack the agent may also hijack the evaluator - so the failure would be correlated in the direction that hides it. Three findings. Agents solve **under 66%** of these tasks with **no attacker present**. **More capable models are measurably *easier* to attack** (inverse scaling), because weak models fail at the attacker's goal too - *the safety of a weak agent is incompetence, not robustness*. And attack success is a property of **the application, not the model**: ~92% on Slack against 0% on some Travel tasks, predicted by how much of the tool output the attacker controls. On defences, the least sophisticated option wins - a **tool filter** (choose tools before seeing untrusted data) drops attack success to **7.5%** at high utility, against a BERT detector that matches it for ~30 points of utility. | agent-security, evals, agents | When you need to **measure** rather than argue, and before adopting any injection defence. Read §3 and §8 of the note if nothing else - §3 is the deterministic-judge argument, which transfers to any adversarial evaluation you build, and §8 is **the 17%**: the tool filter fails when the task's own tools also suffice for the attack, which is the structural bound on **every** isolation defence here, CaMeL's policies included. Also the closest available thing to a **verifier for the security half** of a self-improving agent. **⚠️ Shares two authors with S18 (Debenedetti first-authors both, Tramèr co-authors both), so it CANNOT validate CaMeL** (`d1`) - that open question stays open, and its corroborating weight lies against S16, S17 and S19 instead. The 8% detector figure travels while its utility cost does not (`d2`); the averaged "under 25% attack success" conceals that the variation is by application (`d3`). Models are two generations old, dating the leaderboard and not the framework - and inverse scaling predicts the staleness runs the alarming way. Repo not cloned. | [`sources/260805_agentdojo/`](sources/260805_agentdojo/LEARNING.md) |

| Defending Against Indirect Prompt Injection Attacks With Spotlighting (Hines, Lopez, Hall, Zarfati, Zunger, Kıcıman - Microsoft, arXiv 2024-03-20) | paper | **The cheap defence most teams actually ship, and the analogy that names its own ceiling.** First it measures the thing everyone tries: **adding "do not obey instructions in the document" to the system prompt is nearly a no-op**, ~60% to ~58% attack success - because a defensive instruction is just another instruction in the same undifferentiated stream. So stop instructing and start **transforming**. **Delimiting** (special tokens at the edges) halves attack success and **its own authors recommend against it**, since an adversary with your system prompt forges the closing delimiter - a boundary an attacker can forge is not a boundary. **Datamarking** (a marker interleaved through the *body*: `In^this^manner^Cosette`) drops it from ~50% to **3.1%** and **costs nothing measurable** across four NLP benchmarks. **Encoding** is best and needs a frontier model. The adversary section is the best engineering in it: assume the system prompt has leaked, therefore **randomise the marker** (`1/N^k`), and never use a transformation the attacker can invert - with ROT13 they write text whose ROT13 image *is* the attack. | agent-security, context-engineering | When pricing defences - this is the **cost anchor** at 1.06x input tokens against CaMeL's 2.82x - and when you want the framing that sorts every defence you hold. Read §7 and §8 of the note if nothing else. **§7 is the payload**: spotlighting is **in-band signalling**, and the authors explain their own ceiling through phone phreaking - in-band multi-frequency stopped *accidental* interference and was defeated *intentionally*, the fix being a separate channel. They name an **out-of-band** analogue as what is actually needed and call it infeasible in current architectures - **and S18 met that requirement one level up, in a program, a year later**. **⚠️ The weakest source in the security set evidentially**: a vendor preprint with **no venue, no code, no dataset**, from the company whose product S17 found filtering the wrong channel. The headline ">50% to below 2%" is a best-case composite (`d2`), the authors **cannot explain why it works** (`n11`), and **every experiment is non-agentic** (`d3`) - the only variant since tested against a real agent is the one they disown. | [`sources/260805_spotlighting/`](sources/260805_spotlighting/LEARNING.md) |

| Darwin Gödel Machine: Open-Ended Evolution of Self-Improving Agents (Zhang, Hu, Lu, Lange, Clune - UBC / Vector Institute / Sakana AI, **ICLR 2026**) | paper | **The best-evidenced source in this brain, and the second primary `autonomous-research-loops` was waiting for.** Schmidhuber's Gödel machine rewrites itself only on a **proof** the change helps, which is why nobody built one. The DGM swaps the proof for **empirical evidence on a benchmark** - and then handles everything that follows from being able to be wrong. It improves its own codebase, so **self-improvement *is* the coding task it is scored on**, and it keeps an **archive** of every agent ever produced, selecting parents by score **and inversely by how little explored they are**. SWE-bench **20.0% to 50.0%** and Polyglot 14.2% to 30.7% over 80 iterations - and **two ablations show both components are load-bearing**: freeze the meta-agent and gains taper off, keep only the latest agent and it plateaus lowest. What it discovers is unglamorous (granular file editing, patch ranking, retry on empty patches) and it **transfers to held-out models, benchmarks and languages** - Claude 3.7 Sonnet, never used during the search, goes 19.0% to 59.5%. | autonomous-research-loops, self-improvement, agent-security | Before building anything that optimises itself unattended. Read §5 and §9 of the note if nothing else. **§5 is the constructive payload** - what must *not* be modifiable: a **viability gate** (only agents that compile and can still edit code are archived, a liveness invariant kept separate from the performance metric) and a **frozen meta-level**, because a system that can rewrite its own selection criteria can rewrite them to prefer itself. **§9 is where this meets the security track**: the builders state that if the benchmark misses a property, "the self-improvement loop could amplify misalignment over successive generations" - which is **S19's V-S5 from the builders' side**, and completes a three-way convergence with S13's observed random-seed result (claim 177). **⚠️ Scope, not soundness, is the caveat**: the **model never changes**, only the scaffolding around it (`d3`); the evaluation is staged on a heuristic noise threshold (`d1`); and "open-ended" is bounded by a **fixed search procedure** (`d4`). Mild commercial position (Sakana AI). **Code is open-sourced and was not cloned** - the cheapest un-taken second leg of the five ingested. | [`sources/260805_darwin-godel-machine/`](sources/260805_darwin-godel-machine/LEARNING.md) |
| Scaling AI Agent Infrastructure with the MCP Stateless updates (Kurtis Van Gent + Alan Blount, Google Developers Blog, 2026-08-05) | blog | **The brain's first *primary* MCP source, and the first anywhere to pin a spec version** (2026-07-28, superseding 2025-11-25). The `initialize` handshake and `Mcp-Session-Id` are deleted and what they negotiated now rides in `_meta` on every request, with routing metadata promoted into mirrored HTTP headers (`-32020` on mismatch) so gateways route and rate-limit **without deep packet inspection**. Round-robin routing, scale-to-zero serverless and invisible failover all fall out of that one change. **The finding is what the article does not say:** statelessness here is **relocation, not elimination** - state moved to the wire (`_meta` forever), to the client (an echoed `requestState`), and to your application (a task store that is still Redis, four sections after a headline bullet saying Redis is no longer needed). | mcp, agent-security, agents | Before deploying any remote MCP server, and before quoting anyone's "MCP is stateless now" summary. Read §5, §6 and §8 of the note if nothing else. **⚠️ T2 vendor writing about a standard it says it led**, and the headline benefit is that MCP now runs well on serverless platforms it sells. **Nothing in it is measured** - no benchmark, no latency figure, no cost comparison, in an article whose entire argument is scale and cost. **Release candidate on beta SDKs**, and it recommends staging rather than production. **Unusually well evidenced for the class anyway**, because its second leg is six printed protocol payloads rather than a diagram - and decoding one of them produced the sharpest thing in the note: `requestState` is **unsigned plaintext JSON guarding a file deletion**, in a document with a security section that never mentions it. **The spec itself was not read**, so that is a documentation gap, not a proven design flaw. | [`sources/260807_mcp-stateless-updates/`](sources/260807_mcp-stateless-updates/LEARNING.md) |

| Hermes Agent Architecture - Part 1: Gateway, Sessions, and the Agent Loop (Vinoth Govindarajan, "The Agent Stack", 2026-08-10) | blog | **The brain's first source on the *operational runtime* of an agent rather than the shape of its loop - and the first practitioner source here analysing somebody else's open-source project instead of selling their own.** One boring task traced end to end through a real agent, finding that the engineering lives entirely in boundaries that are easy to collapse: **routing identity is not conversation identity** (a misrouted message produces a perfectly valid conversation, so nothing errors), **session state is not prompt context**, **session identity is not the execution workspace** ("correct transcript, wrong repository" presents as success), **a tool schema is not an authorization**, **transcript order is not side-effect order**, and **"the agent succeeded" is not one fact but eight**. The sharpest finding is in a table cell rather than the prose: **the guard stopping two turns from mutating one conversation is memory-only, therefore process-local** - while the article's own closing checklist tells the reader to go determine that for their own system (`d1`). **And an identification that is a first for this brain**: its subject is the same running artifact S19 attacked (claim 196). | agents, context-engineering, agent-security, evals | Before designing the runtime around an agent loop, and **before writing a runbook** - §8's boundary-per-failure table is the shape to copy. Read §7 and §8 of the note if nothing else. Also read it for what the *other* 23 sources here do not cover: identity, persistence, idempotency and delivery. **⚠️ T4 practitioner blog and nothing in it is measured** - no latency, error rate, incident or comparison, and six of its positions are recommendations resting on no outcome. Both corroboration legs are one author's prose against the same author's diagrams, so `corroborated` means internally consistent and nothing more. **It also opens by constructing a deterministic test task and never shows it running** (`d3`), and its stated verification method against source and regression tests is unverifiable from the article - the repo was not cloned. **Read `d5` before using it against S19: Part 1 is not about memory and moves neither claim 160 nor 161.** No topic was created for this material - see [ADR-0023](brain/decisions/0023-no-topic-for-agent-runtime-operations.md), which records the trigger that would. | [`sources/260814_hermes-agent-architecture-p1/`](sources/260814_hermes-agent-architecture-p1/LEARNING.md) |

| Patterns for Building Cybersecurity Evals (Eugene Yan, eugeneyan.com, 2026-06) | blog | **The brain's first *survey* source, and the first to measure agents as attackers rather than as targets.** Seven benchmarks read at once - Cybench, CVE-Bench, CyberGym, ExploitGym, ExploitBench, MHBench and SCONE-Bench - which is the first chance this brain has had to ask what converged across independent eval designs. Three things did, all downstream of one property: exploitation hands you a **free mechanical verifier** (a sanitizer fired or it did not), so success is defined as **a checkable property of the environment rather than of the trajectory** - eight named end states with a grader that interrogates the target and never reads the submission, which means **the grader need not be as capable as the agent** - and it is scored as an **ordered ladder** rather than a bit, because a binary grader reports "found nothing" and "found it and could not weaponise it" identically. **The finding that outranks the survey: every headline number is a configuration, not a capability.** Five dials each moved the score by more than the model-to-model spread - decomposition raised a difficulty ceiling ~11x, swapping the scaffolding took one model from 3 of 40 networks to 37 of 40 with ablations underneath it, and disabling vendor safety filters moved another from 0 to 120 exploits. Capability itself **falls off a cliff** at one rung: reaching the buggy line is saturated at 41/41, sandbox escape is near-zero, arbitrary code execution is zero for every publicly deployed model. | evals, agent-security, agents | Before quoting any offensive-capability number, and before designing an eval for any open-ended task - §4 and §8 of the note if nothing else. Also the cheapest available lesson in reading a figure before its caption. **⚠️ A secondary source: Yan ran none of these experiments, every number belongs to six arXiv preprints and one vendor page, and no primary was fetched** - so its gate establishes a faithful *reading*, never a fact ([ADR-0025](brain/decisions/0025-a-secondary-source-corroborates-its-own-reading.md)). **Five of its eight divergences run one direction, the article understating its own figures**, including an entire experimental arm absent from the prose and a total safety-filter block summarised as partial refusals. **And the headline finding is figure-only**: a chart annotated with a **~1.3-month doubling time** for offensive capability, which the prose never mentions - T2 vendor benchmark, best-of-eight, eight points, so cite that a rate was measured and not the number. | [`sources/260815_cybersecurity-evals/`](sources/260815_cybersecurity-evals/LEARNING.md) |
| LLM Knowledge Bases: a practical guide (Ben Holmes, Warp, AI Engineer World's Fair 2026) | video | **The first independent instantiation of S8 (LLM Wiki), and the cleanest lesson this brain has on why that is worth less than it sounds.** Ben Holmes read Karpathy's `llm-wiki` gist, built it, and demonstrated the working parts - the skill file, the tag registry, a generated entity page, the scheduled job. **Displaying a source is not corroborating it** (claim 212): the gist on the projector is the same leg wearing a different hat, so **no S8 node moved** and the eighteen `needs-check` assertions stayed exactly where they were. What did arrive is the residue - four mechanisms the pattern turned out to need and never mentions. An **idempotence stamp** (`enrichedAt`, skip if present) turning maintenance from O(corpus) into O(new), which is the missing precondition that makes a nightly schedule affordable at all (claim 206). A **controlled tag registry** the agent must read first with an explicit instruction to resist extending it, because each per-note call otherwise coins a locally perfect and globally useless label (claim 207). **Unattended scheduling** with the human meeting the work as a morning diff. And a **per-directory schema** discovered with `find`, overriding the worker's generic instructions (claim 209). **The best finding is the contradiction the talk never notices**: enrichment writes titles and backlinks into the raw notes while the same talk displays "raw sources are read-only" and encodes it in its own automation - and the rule that survives implementation is **one declared writer per layer, with the exception written down** (claim 211). | rag, memory, skills, agents | When you are building or maintaining a knowledge base an agent writes to - and **before citing S8 and this together as two sources**, which is the error it is easiest to make here. Read the `nodes.md` independence section, then sections 5-7 of the note. Section 8's entity page (per-claim citations, claim 210) is the single most transferable frame. **⚠️ Nothing in it is measured** - no baseline, no comparison against the RAG systems it opens by dismissing, no error rate, no corpus size, no cost (`n16`); the only number uttered is an unsourced 200 wpm about typing. **T4 practitioner demo with a T2 commercial interest on its most novel section** (`d2` - the scheduling half runs exclusively on the speaker's employer's product). **Three of its most interesting mechanisms are figure-only** (`n11`, `n12`, `n15`), visible in screenshots and never spoken. **The review step carrying the entire safety argument for unattended operation is one unexamined sentence** (`n13`). | [`sources/260815_llm-knowledge-bases/`](sources/260815_llm-knowledge-bases/LEARNING.md) |

## Topics (living notes)

The compounding synthesis layer - many sources feed each note. See [`brain/topics/`](brain/topics/).

> **Topics are open** - new ones are added as sources introduce a recognizable new area (the
> **architect** persona owns the create-vs-merge call). **Status** advances `seed` (created, no
> source yet) -> `emerging` (one source, needs-check) -> `established` (two or more corroborating
> sources). Six of the notes below (agents, mcp, skills, rag, agent-security, inferencing) were the
> original seeds; the rest were created by ADR as sources arrived. The set is open, not a whitelist.

| Topic | Status | What it covers | Sources feeding it | Note |
|---|---|---|---|---|
| Agents | **established** | Autonomous LLM agents: the four owned parts (prompt/switch/context/loop), structured output, micro agents inside deterministic code, state + pause/resume, humans as tool calls, self-tuning, scaffolding as expiring bets on model limits, **and ablation as the test for whether a bet has expired**, plus **split a loop rather than let it hold two objectives**, and from S9 the **vocabulary** - loop / workflows / harness as three separable purchases, five named orchestration patterns, and the only enumerated harness inventory here, and from S10 **tool-catalog design** - pin the head, retrieve the long tail, and a tool description becoming an index entry, and from S12 the **organisational** shape - one tenancy boundary buying confidentiality, blast-radius and quota isolation together, and partial progress as an agent-shaped failure semantic, and from S13 **autonomy as a deliberately suppressed check-in default** and the two conditions that earn it, and from S15 that **which agent tasks repeated sampling suits is decided by the task's verifiability rather than the agent's design**, and from S17 the inversion this note otherwise lacks - **a more capable agent is a more capable attack payload**, since an injected sentence states only the goal and the agent's own planning supplies the method (claim 147), and from S18 the agent as **a program written once by one model and executed by a deterministic interpreter** - claim 12 argued from security rather than reliability, and a class of scaffolding that does **not** expire on capability, and from S20 the **measured baseline**: agents solve **under 66%** of realistic multi-step tool tasks with **no adversary present**, and attack costs a further 10-25% of utility whether or not the attacker succeeds (claim 168, which is claim 18 quantified), and from S25 **the largest measured scaffolding effect here and the only one with component-wise ablations under it** - one model going from 3 of 40 networks to 37 of 40 with the model held fixed, all ten models tested scoring zero on the old scaffolding - carrying its own bound, since **scaffolding makes existing capability reliable and does not supply missing capability** (claim 201), plus the warning that guidance interventions are **non-monotonic** and must be measured per model (claim 205) | 16 (15 independent) | [`brain/topics/agents.md`](brain/topics/agents.md) |
| Context engineering | **established** | Which tokens reach the model: prompt authorship, context-window ownership, thread-as-typed-events serialisation, token budget as a reliability lever, error compaction, **context anxiety vs context rot (two distinct failures)** ([ADR-0001](brain/decisions/0001-context-engineering-topic.md)), plus **the contract document as the persistent counterpart to owning the prompt** and the two-pass text-then-images constraint (S8), plus **context management as middleware** - compaction, tool selection and permissions applied as policy rather than authored per call (S9), and from S10 **the tool manifest as a measured budget line item** - 36x fewer tokens by deferring it, and caching as a price cut that is not an attention cut, and from S11 **metadata as a control surface** (a column definition carrying a default policy - the claim that completes a three-source pattern), **sorting context by the question it answers**, and **the head/tail inversion** against S10, and from S12 the **session token cap read as a loop guard** rather than a budget, and from S13 the **per-iteration** budget of an unattended loop - a 5-minute run compressed to ~2 grepped lines, with the cost multiplied by the iteration count, and from S18 the strictest answer here to **who may write into the context window** - the planner never sees tool output and the untrusted parser has no free-text channel back, which is claim 22's discipline argued from *authority* rather than from *attention* | 11 | [`brain/topics/context-engineering.md`](brain/topics/context-engineering.md) |
| Evals | **established** | Evaluating agent/LLM pipelines: per-stage metrics, pass@k, pairwise comparison, QA gates, closed-loop self-tuning, generator/evaluator separation and making subjective quality gradable, **plus ablation as a method and gating the diff in CI**, and from S11 the **counter-example** - five instruction artifacts shipped company-wide with no evals - plus two claims about measurement itself: **throughput is cheap to count and correctness is not**, and **benchmark-measured context gains understate production effects** (+2pp vs +16pp), and from S13 the **metric-design** half this note lacked - anti-Goodhart as a code-layout problem, the producer sitting on the *reporting* path even when the scorer is frozen, and **an accept rule with no variance handling banking a random-seed change as an improvement**, and from S15 the **coverage-versus-`pass@1` reporting failure** (an existence claim quoted as a delivered result) plus a scaling exponent that reads as a **diagnostic for your eval set**, and from S20 the **adversarial** half this note lacked - **a model judge is unsound when an attacker is present**, because the attack that hijacks the agent may hijack the judge, so the failure is correlated in the direction that hides it (claim 164, the sharpest form of claim 34 here) - plus **utility and security as two axes rather than one score**, since either alone rewards a degenerate corner, and from S25 the **first survey** - seven cybersecurity benchmarks read at once, converging on **success as a checkable property of the environment rather than of the trajectory** (so the grader need not be as capable as the agent) and on **an ordered ladder rather than a bit**, plus the claim that outranks both: **a capability number in an adversarial domain describes a configuration, not a capability**, with five undisclosed dials each moving the score by more than the model does (claim 197) | 10 (9 independent) | [`brain/topics/evals.md`](brain/topics/evals.md) |
| Memory | **established** | What persists **between** calls and who maintains it: staleness as a structural property of write-once stores, background curation decoupled from the session (and **why - objective conflict, not throughput**), revision over expiry, representation as a maintenance decision, the three-objective evaluation frame, plus the multi-agent half - **memory as a tool-accessible file system, scoped stores, optimistic concurrency, attribution** ([ADR-0007](brain/decisions/0007-memory-topic.md), [ADR-0008](brain/decisions/0008-memory-established.md)). **S8 adds a third, non-vendor and earliest statement of the decoupled pass** ([ADR-0010](brain/decisions/0010-lint-is-the-dream-pass.md)) - corroborating the *practice*, not the rationale. **⚠️ Three sources converged on the design; none published an experiment.** **S16 (2026-08-04) is the first here that did, and it is an attack** - it designs no memory and instead poisons the shape all three share, showing that **one** attacker-written record plus a fluent trigger phrase redirects retrieval, with benign behaviour intact. **S19 is the one that makes this note uncomfortable rather than merely cautious**: holding the model constant, an agent with permissive retention and automatic system-prompt injection of memory is roughly **2x** easier to poison than one that retrieves only via an explicit tool call, so **the design choices that make an agent better at long-horizon work are the ones that make it easier to poison** (claim 160) - and the decoupled background pass this note recommends sits, on S19's map, in two named vulnerability classes. Read it against the two design choices this note treats as settled: a maintained artifact read back as instruction, and retrieval by embedding similarity. **S17 attacks the same store from the opposite direction** - the *agent itself* is instructed to write the injection into long-term memory, so a fresh session reading its own notes is re-compromised and **a session reset does not clear it** (claim 145) | 6 | [`brain/topics/memory.md`](brain/topics/memory.md) |
| MCP | **emerging** | Model Context Protocol. **The protocol core, from S23 (first primary source, and the first to pin a spec version - 2026-07-28):** the `initialize` handshake and `Mcp-Session-Id` deleted, `_meta` carrying the negotiated fields on every request, routing metadata promoted to mirrored HTTP headers, MRTR and the Tasks extension as the two residual-state mechanisms, a 12-month deprecation policy that retires Roots, Sampling and Logging, and RFC 9207 + RFC 8707 as the first authorization mechanism this note has had. **The transferable finding is that statelessness relocated the state rather than eliminating it** - to the wire, to the client, to your application - which is claim 106 reached by a second route. **From S10, the client side:** `tools/list` as a per-turn cost centre scaling with the catalog (541k tokens at 1,180 tools), the guard against calling unregistered tools, aggregating servers, and the indexed-vs-model-facing surface of a tool. **From S12, the deployment side:** MCP as the mandatory data seam, and local-vs-shared placement as an isolation decision. **Still `emerging` at 3, deliberately** - the three sources are disjoint and **no two confirm the same mechanic**, so a primary source fixed the scope defect and not the corroboration defect ([ADR-0022](brain/decisions/0022-a-primary-source-is-not-corroboration.md)). **Resources and prompts remain at zero**, and **nothing in this note is measured by anyone** | 3 | [`brain/topics/mcp.md`](brain/topics/mcp.md) |
| Skills | emerging | Agent skills: the three-layer cost ladder, capability vs preference lifespans, writing a description that triggers correctly (and declaring what must *not* trigger it), evaluating a skill, and **retiring one by ablation**. S7 supplies the category name: **a skill is procedural memory**. Stays `emerging` - S7 names the family, it does not corroborate S5's claims, and S9 adds only an **independent** vendor filing `skills` under Context beside prompts and memory (one box in one diagram), and S11 only calls its business-context documents "like skills for the data agent" in passing - **the first instance outside a coding context, and a live counter-example on evals**. Note the trap: the source count rose 1 -> 5 while the evidence did not move at all ([ADR-0014](brain/decisions/0014-no-topic-for-organisational-context.md)). **S13 adds the second instance outside a coding-assistant context** - a 115-line markdown file the author himself calls "a super lightweight skill", holding an entire research organisation. **S19 supplies the topic's first security material**: skill synthesis is a memory **write channel** (C4) with no content inspection before the file is written, and **V-S5 says a self-improvement loop *optimises* a poisoned skill** - it treats every error-free execution as validation and builds later revisions around the adversarial step. Note the trap: the count moves 5 to 6 and **S19 is not about skills either**, corroborating none of S5's claims | 6 | [`brain/topics/skills.md`](brain/topics/skills.md) |
| RAG | emerging | Retrieval-augmented generation - **and, since S8, its alternative**: maintained knowledge layers compiled once at ingest rather than retrieved per query. Retrieval as *stateless across queries*; layering a store by **write access** (immutable raw / LLM-owned / co-evolved schema); ingest-integrates-not-indexes; queries filed back as pages; the catalog/log split; **maintenance labour as the binding constraint** (Memex, 1945). **Since S10 the topic has actual retrieval mechanics and its first numbers**: sparse lexical retrieval holding its own against a GPU cross-encoder reranker, Recall@k, index-field tuning, and **retrieval quality as an editorial problem before an algorithmic one**. **⚠️ Stays `emerging` on two sources that barely touch: chunking, embeddings, vector stores, hybrid search and grounding evals are still at zero, and S10 retrieves over tool schemas rather than prose**. **S11 adds the topic's first production deployment** of a maintained layer, plus its first externally corroborated claims: a **trust signal needs a writer restriction** (independent prior art in Power BI endorsement, and two tiers beat one), **the query log is the demand signal for what to document**, and the **knowledge acquisition bottleneck** (1977) as a second precedent for claim 72 - the LLM collapsed the *encoding* cost of expert knowledge, not the *elicitation* cost. **S16 adds the topic's first T3 academic source and its first adversarial one**: retrieval geometry as a **control surface an attacker can write into**, which is the adversarial form of claim 91 (retrieval quality is editorial before it is algorithmic) and supplies a second, independent argument for claim 95's writer restriction - this one structural where every detective defence fails | 4 | [`brain/topics/rag.md`](brain/topics/rag.md) |
| Agent security | **established** | Threats + mitigations: prompt injection, tool poisoning, exfiltration - **plus the delegated-authorization substrate** (OAuth/OIDC): scopes as least privilege, consent as human-in-the-loop, channel separation, PKCE. **plus, from S12, containment**: put the boundary where the model has no vote (a project per business unit), bound the **principal** rather than the resource, filter prompts at the network edge - and the claim worth carrying anywhere, that **sharing a component converts a structural guarantee into an enforcement obligation**. Splits into its own `identity-and-authorization` note on a source that teaches identity *mechanics* - **S12 is not it** ([ADR-0015](brain/decisions/0015-an-architecture-is-not-an-identity-source.md)). **Shared agent memory remains the most actionable open threat**; the sharpest open *question* is now what actually propagates an end-user identity to a shared tool server. **And from S16, the topic's first measured attack**: a retrieval store is an input with the properties of a prompt, poisoning it needs **no model access and no training**, the mechanism is geometric (map triggered queries to a unique + compact embedding region), **one record and a one-token trigger are close to sufficient**, triggers transfer to embedders they never saw, and a fluency constraint defeats perplexity filtering by removing the property it measures. Stays `emerging` - **four sources, none corroborating another** - but S16 converts claim 63 from commentary into measurement, and **closes the note's longest-standing open question by demonstrating what it feared**. **And from S17, the general case and the status change**: processing untrusted retrieved data is **analogous to executing arbitrary code**, the adversary never enters the session at all, the classical threat taxonomy transfers wholesale, worms and persistence and C2 are demonstrated on real products, **the attacker states only the goal and the model supplies the method**, and **input filtering fails by sitting on the wrong channel** (Bing filtered chat, not retrieval - which independently confirms this note's own prior commentary on claim 103). **Advanced to `established` 2026-08-04 ([ADR-0019](brain/decisions/0019-agent-security-established.md)) - on the *pair*, not the count**: S16 and S17 corroborate on agent memory as a persistence surface by opposite mechanisms, from unrelated teams. **⚠️ `established` describes coverage of what the note asserts, and it asserts attacks. Everything gated is an attack; there is no gated defence** - ten named defences are now documented as failing and the entire 2023-2026 defensive literature is ungated **And from S18, the note's first gated *defence***: secure the system rather than the model, the Dual-LLM data-flow gap, capabilities carrying provenance and permitted readers checked by policy at every tool call, at ~3x token cost - **but it protects actions and not assertions**, so fraud and manipulated content are explicit non-goals and half of S17's taxonomy stays untouched (claims 149-156). **And from S19 the write surface**: four memory write channels of which **three are inferred rather than commanded**, nine structural vulnerabilities, the strong/weak signal split, and the measurement that **detection-based defences fail and retraining makes the best one worse** - plus V-S5, where a self-improvement loop **optimises** a poisoned skill (claims 157-163). **And from S20 the measurement layer**: the deterministic-judge argument, **inverse scaling** (more capable models are easier to attack), attack success as a property of the **application** rather than the model, and the **tool filter at 7.5% with its 17% structural bound** (claims 164-168). **And from S21 the cheap defence and the framing that sorts them all**: instructing the model is nearly a no-op, transforming beats instructing, **datamarking is ~50% to 3.1% at no task cost**, randomise the marker, and **spotlighting is in-band signalling** - whose authors name out-of-band as the real answer, which S18 then met one level up (claims 169-173, including the **three-class taxonomy**: detection, behavioural, structural - all acting on the **input path**, none surviving a memory write) **And from S22 the amplification mechanism stated by builders** - a self-improving loop compounds whatever its benchmark cannot see (claim 177, corroborating S19's V-S5 non-adversarially)  **S23 (2026-08-07) is the eleventh and joins no corroborating group**, contributing the first spec-level authorization mechanism this note has had - **RFC 9207** issuer verification and **RFC 8707** resource indicators, the audience restriction two sources asked for from opposite directions (claim 182) - and one **fresh** trust surface: MCP's stateless redesign pushes server execution state through the **client**, unsigned in the article's own example and guarding a file deletion, which **inverts claim 29** (the untrusted leg should carry useless material) and defeats claim 28's itemised consent (claim 181). **And from S25 the note's first source on *offensive* capability rather than on attacks against agents** - where exploitation stops (saturated at finding bugs, near-zero at sandbox escape, and the barrier holding is ordinary memory-safety engineering, claim 200), why every published number **describes a configuration end users cannot reach** (safeguards disabled, and the same model scoring 120 has all attempts blocked by default, claim 203), and **the first rate of change attached to any security capability here** - a ~1.3-month doubling, figure-only and vendor-measured (claim 204) | 13 | [`brain/topics/agent-security.md`](brain/topics/agent-security.md) |
| Autonomous research loops | emerging | **New 2026-08-03** ([ADR-0017](brain/decisions/0017-autonomous-research-loops-topic.md)). An agent that iterates on an artifact **unattended**, accepting or rejecting each change against an automated metric - *not* literature-search agents, which is why the name is long. Covers the four things frozen before the loop starts (editable surface, resource budget, metric units, holdout), **anti-Goodhart as a code-layout problem rather than a prompt problem**, version control as the experiment database with rollback as discard, the rollback-safe ledger, the per-iteration context budget, autonomy as a deliberately suppressed check-in default, and the accept rule's statistical behaviour - **including a loop banking a random-seed change as an improvement**. Cross-references claims 7, 10, 31, 34 and 59 from `agents`/`evals` without re-homing them. **And from S22 (Darwin Gödel Machine, ICLR 2026) the note's second primary, resolving its merge-back trigger** ([ADR-0020](brain/decisions/0020-autonomous-research-loops-second-primary.md)): **an archive beats a lineage, ablated** - hill-climbing on the current best agent plateaus lowest, because one bad self-modification damages the only agent you have; parent selection weighted **inversely to how explored a branch is** lets a run cross a valley it would otherwise discard; and two freezes stated with reasons - a **viability gate** separate from the performance metric, and a **meta-level the system cannot modify**, since anything that rewrites its own selection criteria can rewrite them to prefer itself. **The two sources are near-opposites evidentially** - T4 personal repo against ICLR main track with open code and real ablations - and they disagree productively on variance: S13's accept rule ignored it and banked a random seed, S22 designed a staged evaluation around it | 2 | [`brain/topics/autonomous-research-loops.md`](brain/topics/autonomous-research-loops.md) |
| Self-improvement | emerging | **New 2026-08-04** ([ADR-0018](brain/decisions/0018-self-improvement-topic.md)). Systems that improve themselves from their own output: the **loop closure** (sample, select, fine-tune on the survivors, repeat), the **coverage/precision decomposition** that makes it legible, **test-time compute as a third scaling axis**, and the constraint that decides where any of it can run - **verification availability, which sets the ceiling rather than the generator**. Carries the second clause neither source reaches alone: *having* a verifier is not the same as having one that works (claim 124 joined to claim 114). Cross-references claims 12, 34, 59, 113 and 114 without re-homing them. **The model layer, where the autonomous-research-loops note below is the artifact layer** - same shape, different altitude, merge-back trigger recorded in ADR-0018. **S15 (2026-08-04) supplies the mechanism half**: the scaling law and *why* it exists, the **measured** generation-verification gap (0.40 against 0.95 on MATH, and it widens with difficulty), the two-dimensional boundary where test-time compute stops paying, and **fusion beating oracle selection**. **⚠️ 2 sources but still 1 *independent* - S15 is lecture 2 of S14's course**, so the raw count moved and the evidence did not, exactly as this note's playlist warning predicted before it happened. Status deliberately unchanged. **The taxonomy is trustworthy and the measurements are self-reported**. **S22 answers this note's standing open question - *how many turns has anyone run?* - with 80**, cross-referenced rather than re-homed because it sits on the artifact side of [ADR-0018](brain/decisions/0018-self-improvement-topic.md)'s boundary ([ADR-0020](brain/decisions/0020-autonomous-research-loops-second-primary.md)) | 2 (1 independent) | [`brain/topics/self-improvement.md`](brain/topics/self-improvement.md) |
| Inferencing | seed | LLM serving: KV cache, batching, quantization, speculative decoding. **Still zero sources by deliberate call, and the call was re-tested on 2026-08-04 against the hardest case yet.** S15 is a lecture *titled* "Test-Time Compute Scaling" and it still does not qualify: its scope is buying *accuracy* at inference, and it teaches nothing about prefill/decode, the KV cache, batching or serving stacks. Under [ADR-0015](brain/decisions/0015-an-architecture-is-not-an-identity-source.md) a source advances a topic when it **teaches within** the scope, not when it **depends on** it ([ADR-0018](brain/decisions/0018-self-improvement-topic.md)) | 0 | [`brain/topics/inferencing.md`](brain/topics/inferencing.md) |

## Deeper layers (brain-wide content INDEX points into)

- [`brain/claims.md`](brain/claims.md) - cross-source **corroborated claims** with citations.
- [`brain/glossary.md`](brain/glossary.md) - 💡 **terms** defined once, reused across sources.
- [`brain/log.md`](brain/log.md) - dated **ingest milestones**.
- [`brain/decisions/`](brain/decisions/) - **ADRs** for durable structural decisions (architect persona).
- [`brain/dreams/`](brain/dreams/README.md) - **reconciliation passes**: the brain checked against *itself* for contradictions, duplication, stale confidence and superseded framings ([ADR-0009](brain/decisions/0009-dreaming-reconciliation-pass.md)). Run on request with `/dream`; `validate.py` checks form, this checks drift.
- [`reports/`](reports/README.md) - **synthesized cross-source study material** (synthesizer persona). Start with [agent fundamentals ramp-up](reports/260725_agent-fundamentals-ramp-up.md).
- [`brain/reading-list.md`](brain/reading-list.md) - **ingest candidates**: primary sources this brain has decided are worth gating and has not gated yet, ranked by the topic gap each would close. Pointers, never evidence.
- [`experiments/`](experiments/README.md) - **first-party observation**: things this brain **ran
  itself**, to find out what actually happens ([ADR-0024](brain/decisions/0024-experiments-layer.md)).
  Predictions are committed **before** the code, checks are deterministic, and nondeterministic
  behaviour is reported as a distribution. **The hard ceiling: an experiment tests whether a
  *mechanism* is real and reachable, and never corroborates the source that suggested it.** Start with
  [runtime boundaries](experiments/260815_runtime-boundaries/RESULTS.md) - two gateway processes on one
  `state.db` keep perfect database integrity and lose semantic integrity completely, and a delivery
  ledger turns out to close one window while leaving an adjacent one wide open.
- [`foundations/`](foundations/README.md) - **supplied background, uncited by construction**: the fundamentals a reader needs that no source taught. **Not evidence, never promoted to `claims.md`** - the repo-level home for the `Background, supplied` blocks inside each `LEARNING.md`.

## Config (taste + workflow)

- [`AGENTS.md`](AGENTS.md) - behavioral contract + the paste-a-URL ingest rule. Read every session.
- [`personas/README.md`](personas/README.md) - role overlays; auto-loaded per the routing table.
- [`prd.md`](prd.md) - the design + rationale.
