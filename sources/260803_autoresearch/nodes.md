# Knowledge nodes - karpathy/autoresearch

> Persona: **fact-checker** - re-adopt when working this file.

> The atomic units of knowledge extracted from this source. Each node has a **stable ID** (`n1`,
> `n2`, ... - never renumber; topic notes reference these), a claim, **both legs each with its own
> citation** (or one leg marked `single-leg`), a gate verdict, and a confidence. Only
> `corroborated`/`single-leg` nodes (and recorded `divergence` findings) feed `LEARNING.md` and get
> promoted to `../../brain/`. The **fact-checker** persona owns the verdicts; see `../../AGENTS.md`
> and `prd.md` §6.

> **Read this before trusting any verdict below.** For a code source the two legs are **docs vs
> code**. Here the "docs" leg is `README.md` and `program.md`, both written by the same author in
> the same repository as the code - so `corroborated` means **the instructions and the
> implementation agree**, which is a real and useful check (it caught `d2`) and is emphatically not
> external validation. Nothing in this table has been confirmed by anyone outside the repository.
>
> **A second, sharper limit.** The source's only *empirical* content is one PNG. `results.tsv` is
> untracked by design ([`program.md:102`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L102)),
> so the underlying data does not exist in the repo. Nodes `n12`-`n14` are read off a **rendered
> chart** by eye; their numeric values are approximate and are labelled so. Nothing here is a
> reproduction - **this brain did not run this code** (no GPU; see `SOURCE.md`).

Permalink base: `https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/`

## Nodes

| ID | Claim (crux) | Evidence leg + citation | Corroborating leg + citation | Gate | Confidence |
|---|---|---|---|---|---|
| n1 | **The agent's editable surface is exactly one file; everything defining the experiment is declared read-only.** The separation is a *declaration*, not an enforcement - no sandbox, checksum or import guard exists. | Code: constants sit under a banner comment `Constants (fixed, do not modify)` and are imported by the editable file - [`prepare.py:26-32`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L26-L32), [`train.py:26`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L26) | Docs: "Modify `train.py` - this is the only file you edit... **CANNOT** Modify `prepare.py`. It is read-only." - [`program.md:25-31`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L25-L31); [`README.md:13-15`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L13-L15) | corroborated | OK |
| n2 | **Train/validation separation is enforced structurally, in the read-only file** - one shard is pinned as validation and excluded from tokenizer training and from the train dataloader. This is the one rule the agent cannot break without editing a forbidden file. | Code: `VAL_SHARD = MAX_SHARD` [`prepare.py:42-44`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L42-L44); excluded from tokenizer corpus [`prepare.py:127`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L127); excluded from the train split [`prepare.py:259-263`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L259-L263) | Docs: section banner `Evaluation (DO NOT CHANGE - this is the fixed metric)` [`prepare.py:339-341`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L339-L341); "The `evaluate_bpb` function in `prepare.py` is the ground truth metric" [`program.md:31`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L31) | corroborated | OK |
| n3 | **The held-constant resource is wall-clock training time, not steps and not tokens** - 300 seconds, with the first 10 steps excluded from the accounting so compilation is not charged to the budget. | Code: `TIME_BUDGET = 300` [`prepare.py:31`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L31); `if step > 10: total_training_time += dt` [`train.py:578-579`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L578-L579); loop exit [`train.py:603-604`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L603-L604) | Docs: "a **fixed 5-minute time budget** (wall clock, excluding startup/compilation)" [`README.md:17`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L17); rationale at [`README.md:64`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L64) | corroborated | OK |
| n4 | **The metric is chosen to be invariant to what the agent may change**: bits-per-byte normalises by *bytes* rather than tokens, and evaluation always runs at the fixed `MAX_SEQ_LEN` whatever the model trained at. | Code: byte-normalised, special tokens masked out [`prepare.py:343-365`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L343-L365); docstring "Uses fixed MAX_SEQ_LEN so results are comparable across configs" [`prepare.py:350`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L350) | Docs: "**val_bpb** ... vocab-size-independent so architectural changes are fairly compared" [`README.md:17`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L17) | corroborated | OK |
| n5 | **The protected metric reaches the scoreboard through agent-editable code.** `evaluate_bpb` is frozen, but the editable file calls it, formats the number, and prints it - and the agent's score is read from that print. The producer is the one reporting its own grade. | Code: imported [`train.py:26`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L26); called [`train.py:613`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L613); printed [`train.py:621-630`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L621-L630) | Docs: the agent reads its result by grepping that print - "You can extract the key metric from the log file: `grep \"^val_bpb:\" run.log`" [`program.md:58-62`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L58-L62), [`program.md:100`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L100) | corroborated | OK |
| n6 | **Version control is the experiment database**: a branch per run, a commit per experiment, and `git reset` as the discard operation. There is no experiment-tracking system. | Docs: branch created at setup [`program.md:9-10`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L9-L10); the loop commits, then advances or resets [`program.md:96-104`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L96-L104) | *(none - git is driven from the agent's shell; there is no code leg in this repo)* | single-leg | needs-check |
| n7 | **The results ledger is deliberately untracked.** The loop rewinds the working tree on every rejected experiment, so the record of what was tried is kept outside the thing being rewound. *(The instruction is the source's; the "because it would be rewound" is this brain's reading - the source never states a reason.)* | Docs: "do not commit the `results.tsv` file, leave it untracked by git" [`program.md:102`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L102); discard is `git reset` [`program.md:104`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L104) | Repo state: no `results.tsv` at the pinned SHA, while `analysis.ipynb` reads one from the working directory - [`analysis.ipynb` cell 1](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/analysis.ipynb) (`pd.read_csv("results.tsv", sep="\t")`) | corroborated | OK |
| n8 | **The per-iteration context budget is engineered down to about two lines.** Three independent mechanisms serve it: the training log is a single carriage-returned line, `tee` is forbidden, and the result is read with `grep` rather than by opening the file. | Docs: "redirect everything - do NOT use tee or let output flood your context" [`program.md:99`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L99); read via `grep "^val_bpb:\|^peak_vram_mb:"` [`program.md:100`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L100); `tail -n 50` **only** when the grep is empty [`program.md:101`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L101) | Code: progress printed with a carriage return and no newline, so the run collapses to one rewritten line [`train.py:590`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L590); machine-greppable summary block [`train.py:621-630`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L621-L630) | corroborated | OK |
| n9 | **Autonomy is an explicit instruction, and it is written as a prohibition on the agent's default behaviour** - "do NOT pause to ask the human if you should continue" - with a stated reason (the human is asleep) and a fallback for idea exhaustion ("think harder", re-read the files, combine near-misses, try radical changes). | Docs: [`program.md:112`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L112), use case at [`program.md:114`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L114) | *(none - no code leg; the agent is not in this repository)* | single-leg | needs-check |
| n10 | **The loop is given a second objective (simplicity) and the conflict is resolved by publishing exchange rates, not a preference.** Four worked trades are given, including "a 0.001 improvement from *deleting* code? Definitely keep" and "an improvement of ~0 but much simpler code? Keep". | Docs: [`program.md:37`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L37); VRAM as a third, soft constraint [`program.md:35`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L35) | *(none - not expressed in code; `results.tsv` has no complexity column, [`program.md:71`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L71))* | single-leg | needs-check |
| n11 | **The accept rule is a bare scalar comparison with no variance handling - and the source's own run demonstrates it banking noise.** The rule is "if val_bpb improved (lower), advance the branch"; the 15th and final kept improvement in the author's run is **"random seed 42->137"**. | Docs: "If val_bpb improved (lower), you *advance* the branch... If val_bpb is equal or worse, you git reset back" [`program.md:103-104`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L103-L104) | Evidence: `visuals/progress_endgame.png` (crop of `progress.png`) - the final green "Kept" point is annotated `random seed 42->137` | corroborated | OK |
| n12 | **That seed experiment is a free measurement of the loop's noise floor, and several other kept improvements sit at or below it.** Reseeding bought roughly **0.0005** bpb; three accepted changes (`short window 1/8 context`, and two of the three RoPE base-frequency steps) appear to be **~0.0002-0.0003** - i.e. smaller than the run's own demonstrated run-to-run variation. | Evidence: `visuals/progress_full.png` / `visuals/progress_endgame.png`, values **read off the rendered chart by eye** | *(none - `results.tsv` is untracked and absent, so the underlying numbers cannot be checked)* | single-leg | needs-check - **the deltas are approximate chart readings, and one seed change is n=1, so this bounds the noise floor loosely rather than measuring it** |
| n13 | **The search behaves as greedy coordinate descent**: three consecutive kept experiments walk a single hyperparameter monotonically (RoPE base frequency 10000 -> 50000 -> 100000 -> 200000), one experiment per step. The loop structure permits nothing else - each experiment is judged against the current branch tip. | Evidence: `visuals/progress_endgame.png` - three adjacent annotated "Kept" points | Docs (structural): the accept/reject rule compares one run against the current state and advances or resets [`program.md:103-104`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L103-L104); nothing in `program.md` proposes batching or joint search | corroborated | OK - the *pattern* is figure-only; that the design permits only this is structural |
| n14 | **Yield is low and front-loaded**: 83 experiments produced 15 kept improvements (~18%), the first four wins land in the first ~8 experiments, and there is a plateau of roughly 22 consecutive experiments with no improvement before the RoPE cluster. Total gain ~0.998 -> ~0.977 bpb (~2%). | Evidence: `visuals/progress_full.png` chart title "83 Experiments, 15 Kept Improvements"; `visuals/progress_early.png` for the front-loading; **bpb values read off the axis by eye** | *(none - underlying `results.tsv` absent)* | single-leg | needs-check |
| n15 | **The shipped figure is filtered, and the filter is in the notebook that draws it**: crashes are dropped and only experiments at or below `baseline + 0.0005` are plotted, while the title counts the whole run. So the chart shows fewer dots than experiments and understates how many attempts went nowhere. | Code: `valid = df[df["status"] != "CRASH"]` and `below = valid[valid["val_bpb"] <= baseline_bpb + 0.0005]` - [`analysis.ipynb` cell 5](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/analysis.ipynb) | Evidence: `visuals/progress_full.png` - title says 83 experiments; markedly fewer points are drawn | corroborated | OK |
| n16 | **The human writes the loop and the agent writes the code** - the inversion is stated as the point of the project: "you're not touching any of the Python files like you normally would as a researcher. Instead, you are programming the `program.md`". The author calls it "essentially a super lightweight 'skill'". | Docs: [`README.md:7`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L7); [`README.md:50`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L50); "**This file is edited and iterated on by the human**" [`README.md:15`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L15) | Repo state: `program.md` contains the entire loop, ledger schema and autonomy policy in 115 lines, and there is **no agent code anywhere in the repository** (10 files, listed in `MAP.md`) | corroborated | OK |
| n17 | **Cheap early abort is built into the artifact**: the training loop kills a run whose loss is NaN or above 100, rather than spending the remaining budget on a doomed experiment. | Code: `if math.isnan(train_loss_f) or train_loss_f > 100: print("FAIL"); exit(1)` [`train.py:570-572`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L570-L572) | Docs: the loop treats an empty grep as a crash and reads the traceback [`program.md:101`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L101); wall-clock kill at 10 minutes [`program.md:108`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L108) | corroborated | OK |
| n18 | **The first experiment is mandated to be a no-op**, establishing the control before any change is attempted. | Docs: "**The first run**: Your very first run should always be to establish the baseline, so you will run the training script as is." [`program.md:39`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L39) | Evidence: `visuals/progress_early.png` - experiment #0 is annotated `baseline` and is itself recorded as a "Kept" point | corroborated | OK |

## Dropped / divergences (audit trail)

| Candidate | Verdict | Note |
|---|---|---|
| `d1` - the crash sentinel | **divergence** | `results.tsv` records crashes as `val_bpb = 0.000000` ([`program.md:76`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L76)) while **lower is better** ([`README.md:17`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/README.md#L17)) - so the sentinel for total failure sorts as the best result in the file. The `status` column disambiguates and the shipped notebook does filter on it, so **nothing in the repo is actually wrong**; the *schema* is, and any consumer that sorts by the metric column inherits the bug. Kept as a finding. |
| `d2` - the "fixed" eval is not quite fixed | **divergence** | `evaluate_bpb` computes `steps = EVAL_TOKENS // (batch_size * MAX_SEQ_LEN)` with integer division ([`prepare.py:354`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L354)) and is called with `DEVICE_BATCH_SIZE` - **an agent-editable constant** ([`train.py:451`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L451), [`train.py:613`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/train.py#L613)). At the default 128 the division is exact (80 steps, 20,971,520 tokens); at 96 it truncates to 106 steps and **20,840,448 tokens, 0.6% fewer**. So the size of the evaluation set moves slightly with a hyperparameter the agent tunes, against the docstring's "so results are comparable across configs" ([`prepare.py:350`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/prepare.py#L350)). Small, real, and **an order of magnitude below the noise floor in `n12`** - recorded for the principle, not the magnitude. |
| `g1` - no cross-run reasoning memory | **gap, recorded not promoted** | The only durable record between experiments is a 5-column TSV whose richest field is a free-text description ([`program.md:71`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L71)). Nothing stores *why* something was tried or what it ruled out. An absence is not a source ([ADR-0012](../../brain/decisions/0012-a-mention-is-not-a-source.md)); discussed in `LEARNING.md` as this brain's reading. |
| `g2` - parallelism hinted, not designed | **gap, recorded not promoted** | A branch name `autoresearch/mar5-gpu0` appears once ([`program.md:92`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L92)) implying one agent per GPU, but there is no mechanism for merging findings across branches and no mention of it elsewhere. |
| `g3` - a bad keep is permanent | **gap, recorded not promoted** | Accepting a change advances the branch, and every later experiment is measured against the new tip ([`program.md:103`](https://github.com/karpathy/autoresearch/blob/228791fb499afffb54b46200aca536f79142f117/program.md#L103)). Nothing re-tests a kept change, so a change accepted on noise (`n11`) silently becomes part of the baseline forever. `program.md:106` permits rewinding "very very sparingly" and gives no criterion for when. |
| `train.py` model internals | **out of scope** | GPT block, MuonAdamW, polar-express orthogonalisation, FA3 kernel selection, value embeddings. Real content, deliberately not traced - the owner scoped this ingest to the research harness (`SOURCE.md`). Not a judgement that it is uninteresting. |
| `prepare.py` data/tokenizer path | **out of scope** | Shard download with retry/backoff, rustbpe training, best-fit document packing. Only the parts that constrain the *experiment* (pinned val shard, fixed constants, the metric) were gated. |
| `uv.lock` | **not fetched** | 443 KB dependency pin; no learning content. |

> **Citations - cite BOTH legs.** Video -> `<youtube-url>&t=<seconds>s`; blog -> `source, <section
> heading>`; paper -> `source, Figure/Table N, §`; **code -> an immutable GitHub blob permalink
> containing the SHA** (`<repo-url>/blob/<sha>/<path>#L<n>`) so it stays inspectable after the local
> `repo/` clone is gone (fall back to `path:line @<sha>` for non-GitHub). A `single-leg` node cites
> its one leg and leaves the other cell `(none)`.
