# Knowledge nodes - Don't Ship Skills Without Evals

> Persona: **fact-checker** - re-adopt when working this file.

> The atomic units of knowledge extracted from this source. Each node has a **stable ID** (`n1`,
> `n2`, ... - never renumber; topic notes reference these), a claim, **both legs each with its own
> citation** (or one leg marked `single-leg`), a gate verdict, and a confidence.

**Base URL for citations:** `https://www.youtube.com/watch?v=0vphxNt4wyk`

> **Gate note - an unusually strong two-leg source.** The deck is ~20 information-dense slides and
> the speaker narrates them closely, so most nodes here are genuinely `corroborated`. **The visual
> leg is not decorative: on `n11`, `n13` and `n28` the slide carries numbers the narration never
> speaks**, which is the concrete argument for having overridden the static probe (see `SOURCE.md`).
>
> **On citing slides whose frame was not kept.** The evidence leg is the *slide*, not the *file*.
> Where a slide agrees with the narration but adds nothing the transcript does not already carry, the
> frame was pruned and the leg is cited as `slide "<title>" @ &t=Ns (frame not kept)`. Downgrading
> those to `single-leg` would have been the dishonest alternative: two legs did agree, and the gate
> records agreement, not file retention.

## Nodes

| ID | Claim (crux) | Evidence leg + citation | Corroborating leg + citation | Gate | Confidence |
|---|---|---|---|---|---|
| n1 | Skills are near-universally used and near-never tested: SkillsBench indexed 47k+ unique skills across 6,300 repos, almost none with tests. | `visuals/frame_60.jpg` - slide "Vibe Checks Fail in Production": "SkillsBench indexed 47k+ unique skills across 6,300 repos. Almost none have tests." | narration "index like over like 50,000 skills ... almost none of those skills had evals" @ `&t=54s` | corroborated | OK (speaker rounds 47k to "over 50,000" - same order, noted) |
| n2 | Non-determinism makes skill quality **unattributable** without evals: you cannot tell whether a task failed because the skill is bad or because the task was too hard for the model. | narration "you might not know if your task fails because your skill is bad or ... because it's way too challenging for the model" @ `&t=72s` | (slide states the adjacent point - "Bad skills don't crash; they quietly corrupt outputs" - not the attribution problem) | single-leg | needs-check |
| n3 | **The reliability bar rises with the user's distance from the skill system.** Agents-we-use tolerate failure because an engineer is in the loop; agents-we-build do not. | `visuals/frame_110.jpg` - slide "Agents We Use vs. Agents We Build": "The further the user is from the skill system, the higher the reliability bar. Automated evals become essential." | narration "there's a big difference between the agents we use and how we use skills and the agents we build" @ `&t=126s` | corroborated | OK |
| n4 | In agents-we-use the **human is the fallback** (you notice a mis-trigger, stop, reprompt, or fire a slash command); in agents-we-build there is no fallback and the end user leaves on first failure. | `visuals/frame_110.jpg` - columns: "You say 'No, use /commit-skill' and it recovers" / "End users who leave on the first failure", "Model-invoked only (no human fallback)" | narration "if your agent does not invoke the skill on the first time, you will notice it very quickly ... they have no idea about what a skill is" @ `&t=109s`,`&t=126s` | corroborated | OK |
| n5 | A skill is a folder with a `SKILL.md` plus assets, working by **progressive disclosure** in three layers: frontmatter (always in context), body (on trigger), references and scripts (on demand). | `visuals/frame_500.jpg` - slide "Keep It Lean (Layer Information)": "Layer 1: Frontmatter (Always Loaded) / Layer 2: SKILL.md Body (Loaded on Trigger) / Layer 3: References & Scripts (Loaded on Demand)" | narration "skills work on progressive disclosure ... title and a description ... second layer is we have a skills body ... then you can really go deep in those reference files" @ `&t=159s`,`&t=176s` | corroborated | OK |
| n6 | The three layers are a **cost ladder**: the description is paid on *every* model call, the body on every trigger, and references cost nothing until the agent explicitly reads them. | `visuals/frame_500.jpg` - "Frontmatter ... sits in context on every turn - keep it minimal"; "External references incur zero context cost until the agent explicitly reads them" | narration "the description is the cost you always pay on every model invocation ... you always pay that 100 200 tokens cost" @ `&t=471s`,`&t=489s` | corroborated | OK |
| n7 | **Two kinds of skill with opposite lifespans.** Capability skills teach what the model cannot do consistently *yet* and are temporary; preference skills encode team workflow and convention and are durable. | `visuals/frame_200.jpg` - slide "Capability vs. Preference Skills": "Teach models what they can't do consistently yet / Temporary (retire as models improve)" vs "Encode team workflows and conventions / Durable (must match team process)" | narration "capability skills teach models something they cannot do consistently at the moment ... those are temporary ... preference skills, those are more durable" @ `&t=194s`,`&t=213s` | corroborated | OK |
| n8 | **Evals are the retirement signal**: they are what tells you a capability skill is no longer earning its context. | `visuals/frame_200.jpg` - "Evals tell you when to retire a capability skill" | narration "Evals will tell us when we can retire skill and when not" @ `&t=213s` | corroborated | OK |
| n9 | Curated skills raise task resolution from **33.9% to 50.5% (+16.6 points)** on SkillsBench 1.1, across open and closed models and multiple harnesses. | `visuals/frame_265.jpg` - slide "Do Skills Work? (SkillsBench 1.1)": "Curated skills jump resolution rate from 33.9% -> 50.5% (+16.6 pts)" + per-model bar chart | narration "skills on average improve the performance by roughly 15%" @ `&t=266s` | corroborated | OK (spoken "roughly 15%" vs slide "+16.6 pts" - speech rounding, not a conflict) |
| n10 | **AI-generated skills are worse than none in places**: self-generated skills cost 8.1 to 11.5 accuracy points; human-written skills perform best. | `visuals/frame_310.jpg` - slide "Self-Generated & Bloated Skills Fail": "Self-generated skills hurt accuracy: -8.1 to -11.5 point loss" | narration "human-written skills are the best we can provide. AI-generated skills can impact performance negatively" @ `&t=299s`,`&t=315s` | corroborated | OK |
| n11 | **Skill length has an inverted-U, and the peak is not "shorter is better".** Compact (<200 lines) +19.0%; **Standard (200-500) +21.5%, the sweet spot**; Detailed (500-1000) +14.5%; Comprehensive (>1000) **+0.7%, a no-op**. | `visuals/frame_310.jpg` - "SKILL LENGTH VS. PERFORMANCE LIFT" panel, four bands with lifts | narration "skills.md files should be below 500 lines" @ `&t=315s` | corroborated (**visual refines**) | OK - **the slide carries the curve; the narration states only the 500-line ceiling and never speaks the sweet spot or the >1000 collapse** |
| n12 | **The description is the trigger mechanism, and it causes 50%+ of all skill failures** - the single highest-leverage line in a skill. | `visuals/frame_425.jpg` - slide "Nail the Description (The Trigger)": "The trigger mechanism causes 50%+ of all skill failures" | narration "we have seen 50% of the failures uh because the skill was not triggered correctly" @ `&t=1036s` | corroborated | OK |
| n13 | Rewriting the description **alone** fixed 5 of 7 failures in their evaluation suite. | `visuals/frame_425.jpg` - "Real Result: Rewriting the description alone fixed 5 of 7 failures in our evaluation suite." | (narration states the 50% figure but never this result) | single-leg (visual only) | needs-check |
| n14 | Write **directives, not passive information**, and put the *what* (capability), the *why* and the *when* (trigger) in the description. | `visuals/frame_425.jpg` - "Too Vague" vs "Specific & Actionable" comparison; "Include both the 'what' (capability) and the 'when' (trigger context)" | narration "we should write directives instead of essays ... very important is the why and the how for the model ... and then, of course, the when" @ `&t=420s`,`&t=437s` | corroborated | OK |
| n15 | **If the workflow is fully determined, write a script, not a skill.** A fixed step-by-step process wastes model calls and tokens on something deterministic code already does. | `visuals/frame_560.jpg` - slide "Set the Right Level of Freedom": "If exact step-by-step execution is required, write a script instead of a skill." | narration "if the process or the workflow is always the same, you don't need to waste models and tokens for that exercise. You can create a script" @ `&t=558s` | corroborated | OK |
| n16 | Over-specifying steps **strips the agent's ability to adapt or recover**; define goals and constraints and let the model choose the path. | `visuals/frame_560.jpg` - "Dictating every step strips an agent's ability to adapt, recover from errors, or find better approaches"; "Provide constraints, not procedures" | narration "rather define goals and constraints ... The model knows what to do. Just like hey, if we need to change the config, here's the file, make the change" @ `&t=558s`,`&t=577s` | corroborated | OK |
| n17 | **Negative cases prevent trigger hijacking**: a broad description ("use for any web dev task") fires on unrelated work and confuses the agent. | slide "Don't Skip Negative Cases" @ `&t=610s` (frame not kept - restates narration): "Prevent trigger hijacking by defining when skills must NOT fire" | narration "if we have a description ... which says use it for web development tasks, it might over trigger ... but if you are very specific for like hey, only use that skill for React components" @ `&t=594s`,`&t=611s` | corroborated | OK |
| n18 | Start with **10-20 real prompts**, split 5 happy-path / 5 negative or near-miss / 5 production traces - real user traces beat synthetic guesses. | slide "Test Early with 10-20 Real Prompts" @ `&t=650s` (frame not kept): "5 Golden Happy Path Cases / 5 Negative & Near-Miss Cases / 5 Production Traces" | narration "always try to create 10 of 20 prompts. I like to create five for like the happy path ... five when I don't want to use that skill ... if you have already some customer or production traces, try to include those" @ `&t=628s`,`&t=645s` | corroborated | OK |
| n19 | **No-ops** - instructions that do not change agent behaviour ("write clear high-quality code") - are common in AI-authored skills and burn reasoning tokens for nothing. | slide "Kill No-Ops in Skill Files" @ `&t=685s` (frame not kept): "A no-op is an instruction that does nothing to alter the agent's behavior"; "No-ops burn reasoning tokens uselessly and obscure real instructions" | narration "AI generated skills tend to include a lot of no-ops ... it's like before making an implementation easy to read ... that's what we expect from the model to do without telling it" @ `&t=680s`,`&t=697s` | corroborated | OK (credited to Matt Pocock) |
| n20 | **Ablation is the retirement test**: run the eval with and without the skill loaded. Active skill 94% vs 32% without (keep); redundant skill 96% vs 95% without (retire). | `visuals/frame_720.jpg` - slide "Retire Skills When Base Models Catch Up", ablation decision table | narration "always try to run evals with and without the skill enabled. And if the model achieves the performance without even like triggering the skill, you know you can retire that skill" @ `&t=713s`,`&t=1268s` | corroborated | OK |
| n21 | **Keep the eval after you retire the skill** - it becomes a regression detector on the base model, and tells you when to reintroduce the skill. | narration "if your model is good enough that it doesn't need the skill anymore, keep that eval ... as soon as you start seeing some degradation, you can reintroduce the skill" @ `&t=1181s`,`&t=1199s` | (slide covers retirement, not eval retention) | single-leg | needs-check |
| n22 | **Grade outcomes, not paths.** Do not assert the skill loaded on turn one; assert the task was achieved. Loading on turn five is still a pass. | slide "10 Best Practices for Skill Evals" @ `&t=1100s` (frame not kept): "Grade outcomes over paths" | narration "we don't want to test if the model loads the skill on like the first turn. We really want to test if it can achieve the task ... if it loads the skill after five turns, that's also okay" @ `&t=1091s`,`&t=1109s` | corroborated | OK |
| n23 | **Run every case in an isolated, clean workspace** - coding agents cheat, reading prior chats or executions to obtain the skill's content without invoking the skill. | `visuals/frame_950.jpg` - "Automatically spins up isolated, clean workspace environments for every test run" | narration "coding agents are very good at finding or cheating ... it might look up previous chats ... and then try to cheat it and get the context from the skill without even using the skill" @ `&t=1109s`,`&t=1129s` | corroborated | OK |
| n24 | Run **multiple trials per case** (they use up to six) and report reliability, because agents and models are non-deterministic. | narration "always run the to six uh trials per case and to measure reliability" @ `&t=1146s` | (not on a kept or observed slide in detail) | single-leg | needs-check |
| n25 | **Test across harnesses, not just yours.** Harnesses behave differently: a skill can be good on Gemini CLI and bad on Codex, and your users may be on the one you never tested. | narration "test across different harnesses ... agent harnesses behave differently and of course model behaves differently. So, maybe your skill is very good with a Gemini but very bad with Codex" @ `&t=1146s`,`&t=1163s` | (no corresponding slide observed) | single-leg | needs-check |
| n26 | **Google DeepMind gates skill changes on evals in CI**: evals live alongside every internal skill, run on every diff, and a change cannot merge unless it improves the test cases. | `visuals/frame_950.jpg` - slide "How we eval skills at Google DeepMind": "Requires proof of positive Skill Lift before any skill PR gets merged" | narration "we run them on every change to the skill ... the change will not be merged if it is not improving the test cases ... you can only change the skill if it improves the eval or add new evals" @ `&t=1002s`,`&t=1019s` | corroborated | OK |
| n27 | **Most skill evals can be cheap regex asserts** (correct SDK, correct model ID, correct methods, no deprecated patterns); LLM-as-judge is reserved for trace-level or complex checks. | `visuals/frame_950.jpg` - "Script evals or data ... LLM judge criteria"; `visuals/frame_800.jpg` - "Eval Suite: 117 test cases across chatbots, streaming, doc processing" | narration "most of the tests or evals for skills can be regex ... it's very cheap to run for it well because we don't need to use like LLM as a judge. But of course, you can use LLM as a judge if you have like more complex skills" @ `&t=878s`,`&t=914s` | corroborated | OK |
| n28 | **Case study - the ideal capability skill.** The Gemini Interactions API shipped *after* the models' training cut-off; a skill with 117 test cases took valid-code generation from 39.2% to 91.6% (Gemini 3.1 Pro) and from 4.8% to 93.2% (Gemini 3.0 Pro). | `visuals/frame_800.jpg` - slide "Gemini Interactions API Skill", knowledge-gap resolution table | narration "the Interactions API was released after the last training of Gemini ... we created 117 test cases ... we improved the performance up to like almost 90%" @ `&t=767s`,`&t=787s`,`&t=805s` | corroborated | OK - but **T2 vendor measuring its own product**, single case, self-reported |
| n29 | The eval harness is deliberately small: a JSON/YAML file of test cases (prompt, language, `should_trigger`, expected checks) plus a Python script that runs the coding agent and asserts on the output. | `visuals/frame_950.jpg` - YAML config showing setup files, CLI checks, LLM judge criteria | narration "we basically only needed like two very simple assets ... a JSON file with all of our test cases ... and then we have a very basic Python script which runs a coding agent" @ `&t=825s`,`&t=841s`,`&t=858s` | corroborated | OK |

## Dropped / divergences (audit trail)

**Curation rule applied:** a frame is kept when it **adds** something the transcript does not carry
(a number, a comparison table, a structure). A frame that merely renders what the speaker already
said is pruned, and its node cites the slide by title instead. Eleven of ~20 slides were kept on
that test.

| Candidate | Verdict | Note |
|---|---|---|
| `frame_350` (Triggering Skills: Model vs. User) | dropped | Restates `n4`'s invocation distinction; the kept `frame_110` carries the same split with the reliability principle attached. |
| `frame_610` (Don't Skip Negative Cases) | dropped | Adds no data beyond the narration; cited by title in `n17`. |
| `frame_650` (Test Early with 10-20 Real Prompts) | dropped | The 5/5/5 split is spoken in full; cited by title in `n18`. |
| `frame_685` (Kill No-Ops in Skill Files) | dropped | Restates narration; cited by title in `n19`. |
| `frame_1100` (10 Best Practices) | dropped | Recap slide - every item appears in an earlier node. Speaker says "you don't need to take photos" @ `&t=1036s`. |
| `frame_1250` (3 Things to Do on Monday) | dropped | Call to action, not evidence. |
| "roughly 15%" (spoken) vs "+16.6 pts" (slide), `n9` | **not a divergence** | Speech rounding of the same measurement. Recorded so a later reader does not re-open it. |
| "below 500 lines" (spoken) vs the 200-500 sweet spot (slide), `n11` | **refinement, not conflict** | Both true; the slide is strictly more informative. The *reason* to keep the visual leg on this source. |

> **Source-level caveat carried into every promotion.** This is a **T4** conference talk by a **T2**
> vendor employee (Google DeepMind) who is partly presenting his own team's numbers (`n28`, `n26`).
> SkillsBench (`n1`, `n9`, `n10`, `n11`) is a **third-party public benchmark** and is the strongest
> evidence here; the DeepMind-internal figures are self-reported and unreplicated. Weigh
> accordingly: **the benchmark claims are the ones that travel.**
