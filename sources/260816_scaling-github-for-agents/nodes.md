# Knowledge nodes - Scaling GitHub for your Agents (S27)

> Persona: **fact-checker** - re-adopt when working this file.

> Atomic knowledge units from this source. Stable IDs (`n1`...) - never renumber. Only
> `corroborated` / `single-leg` nodes and recorded `divergence` findings feed `LEARNING.md` and get
> promoted to `../../brain/`.

**Source label: S27.** Base URL for citations: `https://www.youtube.com/watch?v=0n3MKk7r60w`.

## Nodes

| ID | Claim (crux) | Evidence leg + citation | Corroborating leg + citation | Gate | Confidence |
|---|---|---|---|---|---|
| n1 | **MCP is the third generation of extensible function calling for Copilot**, not the first: function calling in GPT-4/3.5 (June 2023) -> Copilot Extensions (May 2024) -> Anthropic releases MCP (Nov 2024) -> VS Code MCP support (March 2025) -> official GitHub local MCP server (April 2025) -> GitHub remote MCP server (June 2025). | `visuals/frame_100.jpg` - slide "GitHub Copilot MCP Timeline: History of extensible function calling with GitHub Copilot", six dated chips | narration "our MCP journey started, at least in public, in April last year... we open-sourced our local MCP in April last year and we've just turned 1 year's old" @ `&t=90s` | corroborated | **OK on the April 2025 local-server date**; the four earlier chips are slide-only |
| n2 | **The open-contribution mechanism that made the server complete made the agent worse.** Being the most-starred repo of its week drew high-volume public contributions that rapidly filled platform coverage to **over 100 tools**, and "after a month or so of new features, agents in some ways were getting worse at using GitHub and context windows were getting blown out quicker." | narration @ `&t=106s` and `&t=121s` | `visuals/frame_345.jpg` - the before/after table's own baseline row, "Number of Tools - Before: 101 tools" | corroborated | OK |
| n3 | **Tool overload is measured, not felt.** LangChain's ReAct agent study (published Feb 2025): more tools means worse performance **across all tested LLMs**; single-domain agents outperform multi-domain agents by **50%+**; agents requiring **3+ steps** degrade quickly. | `visuals/frame_155.jpg` - slide "More tools ≠ better agents", three findings, sourced `gh.io/langchain-benchmarking`, Feb 2025 | narration "LangChain had already produced research they published in February that year, of the exact kind of problems we were seeing. More tools don't make better agents, you know, they get confused and forgetful" @ `&t=136s` | corroborated **within S27** | **needs-check as evidence about the world.** This is S27 faithfully re-displaying a **third party's** study, which is claim 212's pattern - the count moves and the evidence does not. The study itself was not read |
| n4 | **Three configuration-shaped fixes were built and all three failed for the same reason: everyone used the default settings.** Toolsets (group tools by theme), dynamic tool selection (agent discovers a set then enables it in chunks), and an unreleased RAG prototype for semantic tool search. Every one required the user to edit JSON, "and most users just don't." | `visuals/frame_215.jpg` - slide "We Built Solutions", three boxes; `visuals/frame_295.jpg` - the answer slide, "everyone used the default settings" | narration @ `&t=197s` through `&t=259s` | corroborated | OK - **the most transferable node in the source** |
| n5 | **Changing the default is what moved the numbers**: 101 tools -> 52 tools (**49% reduction**) and 64.6k -> 30.3k tokens (**53% reduction**) of initial load, achieved by focusing tools on the general case using observed usage patterns on the remote server and then grouping CRUD tools. | `visuals/frame_345.jpg` - slide "Improvements compared to having all tools enabled", table Metric/Before/After/Reduction | narration "we cut the amount of context used by focusing the tools more specifically to the general case... based on usage to like about 49% reduction of the initial load. And then we subsequently also grouped CRUD tools and brought that down even more" @ `&t=320s` | corroborated | OK on both percentages. See `d1` for the tool count the narration gives |
| n6 | **Output tokens are the larger prize and the talk spends one sentence on them.** Tailoring what `list_pull_requests` returns (reusing an existing `MinimalPullRequest` type) cut **11,779 -> 1,657 tokens at 2 items (85.9%)** and **657,272 -> 153,352 tokens at 100 items (76.7%)**, measured with `tiktoken` (`o200k_base`). | `visuals/frame_370.jpg` - merged PR #2087 "Reduce context usage for list_pull_requests", Token Reduction table | narration "we've also like recently had a massive push to reduce output tokens of a lot of tools as well... just by tailoring exactly what comes with the list pull requests, it's actually lost more than 75% of the tokens used in the output" @ `&t=351s` | corroborated | OK. The **arithmetic joining this to claim 82** (one 100-item response at 657k tokens exceeds the 541k-token 1,180-tool manifest) is **this brain's**, not the source's |
| n7 | **Tool-call success rate is above 95%, and some residual failure is irreducible**: agents do not know which repos they have write permission on, and they still hallucinate. | narration "we made a big push to reduce tool failures as well. And the success rate is roughly, I think, over 95% at this point. But not all failure is preventable cuz agents don't necessarily know which repos they have write permission on. They still hallucinate" @ `&t=397s` | (no visual - no slide states the figure) | single-leg | **needs-check.** Hedged in the delivery ("roughly, I think"), no denominator, no definition of failure, no baseline |
| n8 | **Preventable failure is removed by encoding agent intent into the tool surface** rather than by returning a correct error. Agent pushes to an uninitialized repo -> just initialize it. Agent uses `main` but the default branch is `master` -> use the actual default. The slide's own question: "if intent is clear and fix is unambiguous, why error?" | `visuals/frame_400.jpg` - slide "Papering Over Agent Mistakes", two failure/fix pairs | narration "we've been able to identify significant numbers of areas that could be overcome, mostly by encoding a sort of agent intent into our tool surface" @ `&t=412s` | corroborated | OK on the mechanism. **The slide names its own cost in its title** - "papering over" - and the source never counts what it costs |
| n9 | **Multi-call sequences are absorbed server-side to cut round trips**: "you might have to make five API calls to make it more robust... we do that in the server side to reduce round trips cuz that saves context, saves time, and usually makes a massively better experience." | narration @ `&t=428s` | (no visual) | single-leg | needs-check - no measurement offered |
| n10 | **Tool descriptions are a joint optimisation, so they are evaluated as a classifier rather than tuned individually.** "Instead of micro-optimizing individual tool descriptions, you try to test them against each other to try and make sure that they're called at the right times and not called at the wrong times... the perfect tool description that makes the agent call it all the time is terrible, as is the reverse of that." The artifact is a **per-tool classification report per model**, produced by a **CI workflow** (`dump-context`, `build-mcp`, `run-eval`, `generate-summary`, `comment-summary`, `failure-alerting`). | `visuals/frame_465.jpg` - slide "Evals Let Us Verify", CI job list beside "Model: gpt-4o-mini / Classification Report" listing every tool name; QR to `gh.io/mcp-evals` | narration @ `&t=443s` through `&t=474s` | corroborated | OK on the method. **The results themselves are not shown at readable resolution** and no score is quoted anywhere in the talk |
| n11 | **Plaintext PAT-based MCP authentication is an anti-pattern and it is the user's default rather than the user's choice.** Tokens in the wild are stored where the agent can read them, frequently long-lived and often over-privileged. "End users, I don't think they're choosing this - it's actually hard to make configuration easy and secure at the same time." | `visuals/frame_520.jpg` - slide "Password or PAT based authentication is an anti-pattern" beside a client config JSON with `GITHUB_PERSONAL_ACCESS_TOKEN` in `inputs` and `env` | narration @ `&t=506s` through `&t=522s` | corroborated | OK. No incidence figure is given for how common this is |
| n12 | **GitHub considered Dynamic Client Registration and rejected it**, despite universal expectation that a remote MCP server would support it. The server supports OAuth 2.1, and Sam's team added PKCE support to GitHub's authorization server. | `visuals/frame_636.jpg` - full-screen slide "everyone expected us to support DCR" | narration "we hoped OAuth would be the path of least resistance. And again, perhaps some of you might know what happened. Everyone expected us to support the dynamic client registration" @ `&t=570s` | corroborated | OK |
| n13 | **The reasons for rejecting DCR are operational, not cryptographic**: implemented properly it is "hard not to have unbounded growth of app databases", there are "challenges of how you would bucket them for rate limits", and "there isn't a reliable app identity". Verdict: "a well-intentioned mistake", and "we're not the only authorization server to not support this". **Client ID Metadata Documents** is the expected replacement, unpromised. | narration @ `&t=586s` through `&t=635s` | (no visual - the slide states the expectation, never the reasons) | single-leg | **needs-check, and it is a first-party operator report** from the team that made the call. That makes it authoritative about the decision and not about whether the decision was right |
| n14 | **The published prompt-injection exfiltration against GitHub's MCP server is correct and is not MCP-specific.** Invariant Labs' "GitHub MCP Exploited: Accessing private repositories via MCP" names GitHub's server; Sam concedes the tools enable it if you enable them all, and argues "it applies to almost every agent setup whether they use MCP or not" - the lethal trifecta. "The utility of agents is in direct conflict with protecting this stuff... it's not solved." | `visuals/frame_655.jpg` - the Invariant Labs post displayed full-screen | narration @ `&t=650s` through `&t=696s` | corroborated | **needs-check on the generalisation.** The concession is credible because it costs the speaker something; the "not unique to us" framing is the interested party's framing, and the trifecta reference is to Simon Willison's blog rather than to a study |
| n15 | **Authorization data is a free, per-user, already-correct filter on the tool surface** - so auth solves the two problems the first half of the talk was about. Three mechanisms: a **PAT** filters tools down to the token's scopes immediately, with the user doing nothing; **OAuth** supports step-up, returning a scope challenge that asks the user interactively so the call **continues instead of failing**; **server tokens** (GitHub Actions) have no user, so user-specific tools are hidden automatically. The slide's own summary: "Less context waste. Fewer failures." | `visuals/frame_815.jpg` - slide "Scope Filtering", three boxes plus that summary line | narration @ `&t=744s` through `&t=822s` | corroborated | OK on the mechanism. **No figure is given for how much context scope filtering removes**, which is notable in a talk that quantified the other two reductions |
| n16 | **The production topology is stateless per request and still runs Redis.** A brand-new MCP server instance is constructed in the SDK sense **on every single request**, with tools added at the start according to configuration, policy and scopes; there is **no session affinity**; sessions exist only to recover the self-reported client identity MCP carries, for telemetry. Redis remains as the session store, behind a load balancer fronting N identical server instances, with Splunk / Sentry / Datadog. | `visuals/frame_875.jpg` - architecture slide: Agents -> Load Balancer -> four MCP Server boxes -> GitHub Infrastructure, with Redis Session Store and three observability vendors beneath | narration "we run a completely sort of stateless server setup and we have been using Redis for session storage... we actually make a brand new server instance on every single request and we add the tools to it at the start... we don't have session affinity" @ `&t=822s` through `&t=903s` | corroborated | OK - and it is **first-party production evidence for claim 180**, from an implementer independent of S23 |
| n17 | **The scale the above runs at**: ~**7.34M tool calls per week**, **11.1M** Docker downloads of the stdio server (which is "by far not the most used version"), **28.6k** stars, **3.9k** forks, **324** watching, **126** contributors, **2,302** issues and PRs (over seven a day, every day, for a year). | `visuals/frame_1100.jpg` - the "by the numbers" dashboard slide | narration @ `&t=886s` and `&t=1090s` through `&t=1136s` | corroborated | OK on every figure except the tool-call rate - see `d2` |
| n18 | **MCP apps put a human back in the loop on agent-authored content**, shipped behind a feature flag rather than generally. The client renders an editable form for the AI-generated issue before it posts: "you want to make sure that it's you posting and it's not going to get closed as a sort of bot-generated thing." Distribution is an opt-in **Insiders mode** that flips feature flags for anyone who wants them. | `visuals/frame_955.jpg` - VS Code chat rendering a Write/Preview issue form, "GitHub MCP Server turns 1 year old!", agent set to Claude Opus 4.6 | narration @ `&t=919s` through `&t=979s` | corroborated | OK on the mechanism. **The motivation is social, not technical** - reception by human maintainers - and the source offers no adoption data |
| n19 | **The speaker predicts the ecosystem reverses his central decision.** Predicted: server discovery becomes automatic, tool use becomes compositional "like bash pipes", data is streamed through tools without wasted model calls in between. Named enablers: Cloudflare's code mode, Anthropic's tool search tool API ("just landed in Claude Code a couple of weeks ago"), and an OpenAI equivalent. | `visuals/frame_1000.jpg` - slide "Predictions", three lines | narration @ `&t=979s` through `&t=1026s` | corroborated | OK on the predictions |
| n20 | **"I fully expect that thousands of tools will be normal very soon. We're trying to iron out all the problems that prevented it in the first place, and I'll probably reverse many of the fewer tools decisions."** | narration @ `&t=1011s` | (no visual - the Predictions slide does not say this) | single-leg | **needs-check as a forecast, and it is the strongest caveat in the source because the author states it against his own interest.** Treat every efficacy figure above as scoped to a design its own author expects to unwind |
| n21 | **A spec annotation nobody surfaces forces the server to ship a redundant mode.** Read-only mode is used by roughly **17%** of users and "maps one-to-one to the read-only hint annotation", but "no client exposes that as a method of filtering servers - I think some gateways now do". | narration @ `&t=274s` through `&t=305s` | (no visual) | single-leg | needs-check on the 17%; the mechanism claim is a direct operator statement |
| n22 | **Every proposal so far to add tool grouping to the MCP specification has been rejected.** "For like every proposal so far for grouping to the MCP specification, for various reasons, has been rejected. And there have been several attempts." The speaker's reading is that modes and configuration are "papering over potential gaps in client implementations". | narration @ `&t=259s` | (no visual) | single-leg | needs-check - reasons are not given, and this is one participant's account of a standards process |
| n23 | **Agent-driven load is now a first-order capacity problem for the platform itself.** Displayed: 1 billion commits in 2025, now 275 million per week and on pace for 14 billion this year; GitHub Actions grew from 500M minutes/week in 2023 to 1B in 2025 and 2.1B "so far this week". Sam: "GitHub itself is also facing a new challenge... this is really intense, and it shows no sign of slowing down." | `visuals/frame_1140.jpg` - a displayed tweet from Kyle Daigle (@kdaigle), GitHub | narration @ `&t=1136s` through `&t=1152s` | corroborated **as a display** | **needs-check.** The numbers are a **GitHub executive's tweet re-displayed by a GitHub employee** - one organisation, two mouths, T2 self-report. Nothing attributes the growth to agents; that attribution is the talk's framing |

## Dropped / divergences (audit trail)

| Candidate | Verdict | Note |
|---|---|---|
| `d1` | **divergence, minor, kept** | The slide's table says the new default is **52 tools**; the narration says "I think you get about **40** tools if you use the default configuration" @ `&t=336s`. Both are the speaker's own account of the same number, minutes apart. Most likely the slide is a dated artifact and the count kept falling - he says so explicitly: "you don't need to worry too much about the specifics, this is dated now" @ `&t=305s`. **Cite the 49%/53% reductions, not the absolute count.** |
| `d2` | **internal inconsistency, kept** | The tool-call rate is given three times in one talk: "around **7 million** a week" @ `&t=886s`, **7.34M** on the numbers slide (`frame_1100`), and "fast approaching **8 million** a week" @ `&t=1136s`. The slide is the only precise figure and sits between the two spoken ones, so it is the one to cite. Recorded because a reader quoting the closing line would overstate by ~9%. |
| `frame_155` "60%+" | **corrected before promotion** | At contact-sheet resolution the domain-confusion figure read as 60%+; cropped and upscaled it reads **50%+**. The lower figure is the one gated. |
| ~15 extracted frames | dropped | Stage shots with the slide off-frame, and near-duplicate captures of a slide already kept (`frame_235`, `frame_250`, `frame_325`, `frame_690`, `frame_750`, `frame_845`, `frame_895`, `frame_1020` and others). |
| `frame_560` (MCP Authorization Specification, April 2025) | dropped | The sequence diagram is unreadable at source resolution, and nothing in the prose leans on it that `n12` does not carry. |
| `frame_925` (Insiders Mode QR code) | dropped | A QR code and a URL. `n18` carries the mechanism; a link is not a visual that teaches. |
| Speaker's aside on MCP CLIs (`&t=1182s`, audience Q&A) | dropped | "Take the read-only tools from some MCP wrapped in a CLI and just give it a proper help and see how the agent does - stuff like that is surprisingly effective." Interesting and entirely uncorroborated - an off-the-cuff answer with no measurement. Noted here so a `/verify` pass does not rediscover it as an omission. |

> **Citations - cite BOTH legs.** Video -> `https://www.youtube.com/watch?v=0n3MKk7r60w&t=<seconds>s`.
> A `single-leg` node cites its one leg and leaves the other cell `(none)`.
